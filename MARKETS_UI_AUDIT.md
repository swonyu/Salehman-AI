# Markets-UI audit (wkd0sd05w, 2026-06-22)

14 findings. #1 (brake risk%) + #5 (strip R/week trading-days) FIXED. Others REAL, RE-VERIFY vs source: #2 best-opp no gate verdict, #3 land on ideas + auto-populate, #4 sizer %/floored-$ mismatch.

### ✅ DONE #1 [high] Money-velocity "Brake" is modeled AND labeled at a fixed 1%/trade while the same card honors the user's editable Risk % everywhere else
**fix:** Thread the user's risk into the summary and read it back into the label/VoiceOver instead of hardcoding 1. (1) Line 2249: compute the fraction from sizerRiskPct and pass it — let rf = (Double(sizerRiskPct).flatMap { $0 > 0 ? $0/100 : nil }) ?? 0.01; let s = StockSageExpectedValue.summary(store.ideas, trades: journal.trades, fraction: rf, holds: velocityHolds). The summary signature already accepts `fraction:` (StockSageExpectedValue.swift:223) and stores it as riskFraction specifically 'so the label can't drift' (line 47). (2) Line 2309: replace the literal "at 1%%/trade" with the carried fraction, mirroring playbook() (StockSageExpectedValue.swift:259) — let pct = Int((s.riskFraction*100).rounded()) and print "at \(pct)%%/trade". (3) Line 2312: use the same pct in the accessibilityLabel instead of "1 percent risk". This makes both the drawdown magnitude (ddPct, which already recomputes from the passed fraction) and its wording track the same Risk % the rest of the card uses. Merges three overlapping reports (the high-sev brake drift, the low-sev latent-drift restatement, and the a11y-label half) — one edit closes all of them.
**verify:** Build green: xcodebuild -scheme "Salehman AI" -destination 'platform=macOS' -configuration Debug CODE_SIGNING_ALLOWED=NO build 2>&1 | tee /tmp/salehman_build.log | tail -25. Then run the app, set Risk % to 2 in the velocity card, and confirm the Brake line reads '2%/trade' and the −X% drawdown roughly doubles vs the 1% case (and at 0.5% it halves). Reset to 1% and confirm it still reads '1%/trade'. A unit test can assert StockSageExpectedValue.summary(ideas, trades:, fraction:0.02).riskFraction == 0.02 and .worstRunDrawdownPct ≈ 2× the 0.01 value.

### ⬜ #2 [high] Best-opportunity card lets you size & copy a plan with NO go/no-go gate verdict shown — a setup the system would itself rate 'Don't take this' can be actioned blind
**fix:** Compute StockSageTradeGate inline in bestOpportunityCard and render a compact verdict pill next to the action badge (line 2195-2197). bestOpportunity() (StockSageExpectedValue.swift:152) ranks purely on EV+conviction and never consults the gate, yet the gate can return .blocked on no-stop / risk over the 2% cap / reward:risk < 1 (StockSageTradeGate.swift:49-66). All gate inputs are already in scope here (idea.advice.stopPrice, reward:risk from price/stop/target, riskFraction from sizerRiskPct, daysToEarnings from store.earnings[...]) — identical to the detail sheet's call at line 2715-2724 — and StockSageTodayPlan.build already prints the gate (line 2229-2233 builds it), so the copied clipboard plan shows a verdict the on-screen card hides. When the gate is .blocked, demote the card (warning tint) and replace the 'Size it now' line (2212) with the failing reason so the strongest-EV-but-reckless setup can't be sized blind.
**verify:** Build green. Then craft/inspect an idea whose reward:risk < 1 or whose risk exceeds the 2% cap, confirm bestOpportunityCard shows a 'Don't take' / caution pill and hides or annotates the size line, and confirm the pill text matches the verdict already printed in the copied plan (StockSageTodayPlan.build output). Cross-check the same idea opened in ideaDetailSheet shows the matching tradeGateView verdict.

### ⬜ #3 [high] Markets opens on Watchlist and the single best money move takes 3 actions to even compute — the cross-section velocity/best-opportunity surfaces are blank on launch
**fix:** Make the actionable surface the default and auto-populate it. (a) Persist the section: change `init(qaSection: MarketSection = .watchlist)` and the backing @State to an @AppStorage("marketsSection") (keep the qaSection arg overriding it under --qa) so it survives relaunch and can default to .ideas. (b) In the body .task (line 134), after `await store.refresh()`, also `await store.refreshIdeas()` when store.ideas is empty and not under --qa, so moneyVelocityCard (gated by summary().hasContent, false on empty ideas — StockSageExpectedValue.swift:48) and bestOpportunityCard have content on first paint instead of requiring (1) tap 'Ideas' (2) tap 'Find ideas' (3) wait. This is the highest-leverage UX change: turns 'the single best move' from a 3-step hunt into the first thing shown.
**verify:** Build green. Cold-launch the app (delete the marketsSection default first) and confirm: the money-velocity header card renders populated without any clicks, the Best-opportunity card is visible, and the last-used section is restored after quit+relaunch. Confirm --qa still lands on the requested qaSection and does NOT auto-fire refreshIdeas (captures stay deterministic). QA-capture the Markets landing PNG and read it to confirm the velocity card is non-empty.

### ⬜ #4 [medium] Position-sizer detail panel prints the requested risk-budget % next to the realized (floored) dollars-at-risk, overstating the loss it sits beside
**fix:** Derive the percent from the same value as the dollars. The caption uses (stop, ps.dollarsAtRisk, sizerRiskPct), but ps.dollarsAtRisk is the loss AFTER share count is floored to a whole number (StockSagePositionSizer.swift:28-32), so it is almost always strictly less than the budget, while sizerRiskPct is the requested budget %. Print ps.dollarsAtRisk/acct*100 instead (acct is in scope from the if-let at line 2591): String(format: "Sizes the LOSS: a stop-out at %.2f costs ~$%.0f (%.2f%% of the account). Not a profit promise.", stop, ps.dollarsAtRisk, ps.dollarsAtRisk/acct*100). Alternatively reword to '(at your %@%% target)' so the % is explicitly the budget, not the shown loss. (StockSagePositionSizer.summaryLine at line 41 already keeps these distinct — match that discipline.)
**verify:** Build green. In the detail sheet, set account $10,000, risk %=1, entry 98, stop 95 (riskPerShare $3 → floor(33.3)=33 shares → $99 at risk) and confirm the parenthetical now reads '0.99% of the account', not '1%'. Try a case with a large riskPerShare ($30 → 3 shares → $90) and confirm it reads '0.90%'.

### ✅ DONE #5 [medium] Fast-lane strip's 'R/week' and '$/week' use different trading-day counts, so the two adjacent lines contradict each other for a crypto-heavy lane
**fix:** Make the strip's R line lane-aware to match the dollar line. Line 2382 calls expectedWeeklyR(store.ideas, holds:) with no tradingDays, so it defaults to 5 (StockSageExpectedValue.swift:180), while the very next $/week line (2387) passes tradingDays: tradingDaysForLane(...) which returns 5-7 by crypto share (line 203-208), and expectedWeeklyDollars recomputes R at that larger count (line 190-195) — so an all-crypto lane shows '+5.0R/week' (5 days) above a '$' figure computed at 7 days, up to ~40% higher. Compute the day count once and reuse for both: let td = StockSageExpectedValue.tradingDaysForLane(store.ideas, holds: velocityHolds); then expectedWeeklyR(store.ideas, tradingDays: td, holds: velocityHolds) on line 2382 and pass the same td into expectedWeeklyDollars on 2387 so they stay in lock-step. (The header moneyVelocityCard already does this — line 2235/2274 — only the strip omits it.)
**verify:** Build green. With an all-crypto fast lane, confirm the strip's R/week and $/week lines correspond to the SAME day count (the $ equals R × dollar-value-of-1R, no ~40% gap). With an equity-only lane confirm nothing changed (still 5 days). A unit test can assert expectedWeeklyDollars(allCrypto, ...) == expectedWeeklyR(allCrypto, tradingDays: tradingDaysForLane(allCrypto)) × account × riskFraction.

### ⬜ #6 [high] Gain/loss on every money card is conveyed by color alone (soft-green vs pure red) — fails for color-blind users and depends on a weak +/- glyph
**fix:** Add a non-color signal to each P&L figure, mirroring signalCard (line 1763) which already pairs an up/down arrow with the color. Prefix portfolioSummary (576), positionRow (648-649), parityRow (763-765), and rebalance trades (733) with an explicit ▲/▼ (or an up/down SF Symbol) and force an explicit sign + bolder weight so a deuteranope — for whom successSoft green (0.45,0.85,0.55) and danger pure-red are the classic confusable pair — can read gain vs loss without relying on hue. Pair with rank 7 (calibrated loss color).
**verify:** Build green. Run QAColorVision over portfolioSummary/positionRow/parityRow (salehman-color-vision-qa) and confirm each P&L row carries a shape/arrow cue independent of color. Simulate deuteranopia on the captured PNG and confirm a loss row is still distinguishable from a gain row with color removed.

### ⬜ #7 [medium] Loss color is raw Color.red — borderline contrast on the dark card fill and the weakest channel for deuteranopia
**fix:** DS.Palette.danger = Color.red is used for every loss figure on the money cards (MarketsView lines 576,649,733,765 and many danger ideaMetrics). The code already acknowledges raw saturated red/green is borderline on the dark surface — actionTextColor mitigates badges (line 3055-3059) and the heatmap adds a text shadow (line 1600) — but the P&L rows get none of it. Add a calibrated `dangerSoft` (brighter/desaturated red tuned for AA on cardFill, analogous to the existing successSoft) and use it for money loss text, paired with the rank-6 arrow/sign fix so color is never the sole signal. Keep raw danger where a shadow/badge already mitigates.
**verify:** Build green. Run QAColorVision contrast probe on a loss figure over cardFill and confirm the new dangerSoft meets AA (the raw Color.red baseline should fail or sit borderline by comparison). Visually confirm losses remain clearly 'bad-colored' but legible.

### ⬜ #8 [high] Portfolio summary money card has zero accessibility labels — VoiceOver reads number fragments with no currency
**fix:** portfolioSummary — the most prominent money card — has no .accessibilityElement/.accessibilityLabel, so VoiceOver announces 'Portfolio value' / '12345.67' / 'Total P&L' / '+200.00 (1.6%)' as disconnected fragments, and the value at line 566 (String(format: "%.2f", t.value)) has no currency word at all. Wrap the HStack in .accessibilityElement(children: .ignore) and add a composed label that runs t.value/pl through a currency formatter and speaks the sign as a word: .accessibilityLabel("Portfolio value \(currency(t.value)). Total P&L \(up ? \"up\" : \"down\") \(currency(abs(pl))), \(String(format: \"%.1f\", plPct)) percent") so the unit and direction don't depend on color.
**verify:** Build green. Enable VoiceOver (or run the Accessibility Inspector / accessibility audit) over the portfolio card and confirm a single combined utterance that includes a currency unit and the spoken word 'up'/'down', not four bare-number fragments.

### ⬜ #9 [high] positionRow is not grouped for VoiceOver — per-holding value and P&L read as loose fragments with no currency
**fix:** positionRow has no .accessibilityElement(children: .combine/.ignore) on the row (only the trash Button at 659 is labeled), unlike tradeJournal rows (1374-1377) and signalCard (1828-1829). Add .accessibilityElement(children: .ignore) + a composed .accessibilityLabel mirroring the journal rows: 'AAPL, 10 shares, value \(currency(value)), \(up ? "up" : "down") \(currency(abs(pl)))', handling the price==nil case with 'no live price' as the visible text already does. Speak the sign as a word so gain/loss isn't color-only here either.
**verify:** Build green. VoiceOver over a holding row announces one combined utterance with currency and spoken direction; the no-price holding announces 'no live price'. Accessibility audit shows the row as a single element, not 4 loose nodes.

### ⬜ #10 [medium] summaryStat sub-line is hard-coded green regardless of the value's sign — a negative EV/velocity renders as if it were good
**fix:** summaryStat always colors the `sub` line DS.Palette.successSoft (line 2350), but moneyVelocityCard passes signed values into it — '%+.2fR EV' (2263), '%+.2fR/day' (2266), and the non-numeric 'if you run top 3' (2269). A negative EV or velocity therefore shows green, miscommunicating a bad number as good (and a false-positive cue for color-blind users). Color the sub by sign: successSoft when the value >= 0, danger (or rank-7 dangerSoft) when < 0, .secondary for the non-numeric caption — or pass an explicit color/role parameter from each caller. Pair with an arrow per rank 6 so it isn't color-only.
**verify:** Build green. Construct an idea set whose best EV or fastest velocity is negative and confirm the sub-line renders in the danger color (not green); confirm the 'if you run top 3' caption is neutral/secondary. Positive values still render green.

### ⬜ #11 [medium] Blocked/Avoid/Hold ideas look and behave identically to actionable buys in the Ideas list
**fix:** Every idea renders through the same ideaCard (2041-2121) with identical chrome, full-width tappable surface, Backtest button and EV badge; an .avoid only differs by a low-contrast grey badge (actionColor → Color.white.opacity(0.22), line 3048) and reads as a fully-live trade. IdeaFilter has only .all/.strongBuy/.buys/.sells (line 19-20) — no way to hide avoid/hold noise. De-emphasize non-actionable cards: when action is .avoid/.hold reduce opacity / drop the EV badge / show a small 'Not actionable' tag, and add an 'Actionable only' default filter (or the missing Avoid/Hold case). In ideaDetailSheet, when the action isn't a buy, render an explicit one-line 'No trade — {reason}' banner at the top instead of leaving the user to infer non-actionability from missing Size/Log buttons (isLoggableIdea line 2963; gate gated to buys at 2714).
**verify:** Build green. In the Ideas list confirm an .avoid/.hold card is visibly demoted (lower opacity / no EV badge / 'Not actionable' tag) and that an 'Actionable only' filter hides them. Open a non-buy idea and confirm a 'No trade — reason' banner appears at the top of the sheet.

### ⬜ #12 [medium] Shares + $-at-risk (the decision-critical number) are buried in the detail sheet — read-only Size on the card is the advisor weight, not a position
**fix:** On ideaCard the 'Size' metric (2079-2089) is suggestedWeight %, not shares; the real share count / $-at-risk (positionSizerPanel) lives only in ideaDetailSheet and only when a stop exists, reachable after scrolling past ~10 notes (2747-2932). Since sizerAccount/sizerRiskPct are persisted (@AppStorage 68-69), compute it on the card: add a compact 'X shares · $Y at risk' line to each actionable ideaCard via StockSagePositionSizer.size(account: ..., riskFraction: ...) — the exact call bestOpportunityCard already makes at 2209-2211. In the sheet, hoist positionSizerPanel and the trade gate to the TOP of the scroll (right under the action badge) above the explanatory notes so the act/size decision needs no scrolling.
**verify:** Build green. Confirm an actionable ideaCard now shows 'N shares · $X at risk' without opening the sheet, and that the number matches the sizer panel in the detail sheet for the same account/risk inputs. Open the sheet and confirm the sizer + gate render at the top, above the notes.

### ⬜ #13 [low] Money-velocity card's load-bearing dollar/per-week figures are the smallest text on the card (8-9pt baseline) — inverted visual hierarchy
**fix:** Headline stats go through summaryStat at 14pt (line 2349) while the concrete '+$X/week' estimate (2275-2276) renders at mvFont9 and adjacent velocity lines (2280-2299) at mvFont8 — on a card whose purpose is at-a-glance 'fastest money now', the dollar number is the smallest text. Promote '+$X/week' to at least the summaryStat tier (>=12-14pt) and lift the per-week R sub-lines off the 8pt baseline. Keep @ScaledMetric (so Dynamic Type still scales) but raise the base sizes for the load-bearing numbers.
**verify:** Build green. QA-capture the velocity card and confirm the '+$X/week' figure is visually at least as prominent as the summaryStat values; confirm at the largest Dynamic Type setting nothing clips or overlaps.

### ⬜ #14 [low] Journal drawdown + growth what-if are fixed at 1%/trade — internally consistent but not the user's configured risk (label-honesty polish)
**fix:** These two journal lines model risk at a fixed 0.01 with matching '1%/trade' wording and are internally CONSISTENT (computation matches label) and explicitly framed as hypothetical ('Stay in the game...', 'What-if (HYPOTHETICAL)...'), so this is NOT a drift bug like the velocity brake — it's optional polish. A trader running 2%/trade reads survival/compounding figures at half their real risk. Either thread rf = Double(sizerRiskPct)/100 into StockSageRiskOfRuin.scenario(... fraction: rf) (1051) and StockSageJournal.projectGrowth(... fraction: rf) (1078) and replace the two literal '1%%/trade' tokens with the computed percent (consistent with the rank-1 brake fix); OR, if the owner wants a standardized reference, reword to 'at a standard 1%/trade reference' so it doesn't read as the user's own risk.
**verify:** Build green. If threaded: set Risk % to 2 and confirm both journal lines recompute and read '2%/trade'. If reworded: confirm the text explicitly says it's a standard reference, not the user's configured risk.
