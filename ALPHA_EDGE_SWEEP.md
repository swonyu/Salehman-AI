# Alpha / edge sweep (wbzkz8s8o, 2026-06-22)

6 VERIFIED alpha specs (31 agents, 2-skeptic). #1 (oversold-bounce knife filter — fixed a REAL gap in my own range-gate) DONE. RE-VERIFY rest vs source.

### ✅ DONE #1 [mean-reversion-quality] — Gate the range oversold-bounce on an intact higher-timeframe uptrend (buy-the-dip, not catch-the-knife)
**mechanism:** EDIT one branch in StockSageAdvisor.advise. Verified against source: the only non-trending BUY is set at StockSageAdvisor.swift:130-131 (`if rsi < 30 { score += 0.25; rangeOversoldBounce = true }`), and the existing 12-1 veto at StockSageAdvisor.swift:177-180 only subtracts 0.20 and runs AFTER the flag is already true — so a name making lower lows can keep rangeOversoldBounce==true and pass the range gate at :198-200, producing a Buy into a downtrend. The double-credit/late-veto gap is real in the current code. Fix: reuse StockSageIndicators.trendOK(_:) (StockSageIndicators.swift:201, returns Bool?) to require an intact uptrend before granting the +0.25 and the flag. trendOK true → unchanged healthy-dip behavior; trendOK false → no credit, honest 'knife not a dip' rationale; trendOK nil (<253 closes) → preserve EXACTLY today's behavior (credit the bounce) so short-history callers stay byte-identical. The only new top-level symbol is the pure helper `oversoldBounceIsBuyable`.

**signature:** nonisolated static func oversoldBounceIsBuyable(_ closes: [Double]) -> Bool { StockSageIndicators.trendOK(closes) ?? true }
// call site replaces the single `if rsi < 30 { ... }` line at StockSageAdvisor.swift:131

**test:** Pure-python re-impl of StockSageIndicators.rsi (sequential, period 14, matching :39-60) + timeSeriesMomentum/trendOK (lookback 252, skip 21, matching :189-203) + the gate. (a) rising 300-bar ramp with a sharp 15-bar tail selloff -> rsi<30 AND trendOK True -> buyable True; (b) 300-bar descending series with oversold tail -> rsi<30 AND trendOK False -> buyable False; (c) 60-bar downtrend -> trendOK nil -> buyable True (legacy preserved). All closed-form, no Swift runtime.

**caveat:** Filters by the name's OWN 12-1 price trend (same TSMOM gate already at :177), not fundamentals or sector/market trend; a fundamentally broken name still above its year-ago price reads 'intact'. Needs >=253 closes; fewer bars => nil => no knife protection (honest, byte-compatible). A filter, never a profit forecast. Not financial advice.

**edgeRationale:** Highest real expectancy AND most buildable: it fixes an existing logic gap (not merely adds a screen) with a one-branch edit reusing a symbol already called 47 lines below. Mean-reversion against trend is the canonical negative-expectancy trap — buying RSI<30 in a name making fresh lower lows averages into a structural decline. Removing the knife sub-population from the buy bucket raises avg per-trade R while cutting trade count (selectivity, not activity), the correct direction for an edge filter.

### ⬜ #2 [earnings-avoidance] — Earnings-gated EV rank key — skip imminent, shrink soon, only with a REAL date
**mechanism:** New earnings-aware overload of rankByEV in StockSageExpectedValue.swift, layered on the existing regimeAdjustedEVRankKey (StockSageExpectedValue.swift:153) and reusing the already-tested StockSageEarnings.severity(daysUntil:) bands (StockSageEarnings.swift:35) — verified present. Discount keyed off an injected [symbol: Date] map: imminent (<=3d) demotes by 1_000_000 (the same sentinel magnitude the file uses at :156 to push a banned side below clean ideas), soon (<=10d) multiplies the positive key by 0.5, clear (>10d) untouched. A symbol ABSENT from the map => treated as clear => byte-identical to today. The DISPLAYED ev/velocity (ev(for:) :177) is never touched — only the ranking key, exactly like the regime gate.

**signature:** nonisolated static func rankByEV(_ ideas: [StockSageIdea], regime: MarketRegime? = nil, earningsDates: [String: Date] = [:], now: Date = Date()) -> [StockSageIdea]

**test:** Python model of the rank key: two byte-identical buy ideas (conviction 0.9, stop 90, target 130, price 100) -> base qualityAdjustedEVR = 1.15432. Assert clear (30d, 1.15432) > soon (7d, 0.57716) > imminent (2d, base-1_000_000); with earningsDates:[:] the order is IDENTICAL to rankByEV(ideas) and rankByEV(ideas, regime: nil) (mirrors the no-op-parity test at :210); a symbol absent from a non-empty map keeps its undiscounted position. Closed-form arithmetic.

**caveat:** Only as good as the date: fires ONLY for symbols whose earnings date is known. fetchNextEarnings (StockSageEarnings.swift:54) is best-effort (equities only, nil when web access off or Yahoo declines), so an unknown date is treated CLEAR and NOT demoted — honest failure mode, but a real imminent earnings the feed missed ranks un-gated. 0.5/skip are policy constants, not a fitted gap distribution.

**edgeRationale:** High expectancy, fully additive, default-param byte-compatible. EV's loss leg is hardcoded at -1R (evR = p*rewardR - (1-p), :78); an overnight earnings gap opens straight THROUGH the intraday stop, so the realized loss leg is fatter than 1R precisely for imminent names — the un-gated EV is biased-high exactly there. Demoting those off the #1 slot raises REALIZED expectancy by not crowning the one idea whose downside the model can't see, without adding turnover.

### ⬜ #3 [volatility-regime] — Breakout qualifier: take Donchian breakouts ONLY in an expanding-vol regime
**mechanism:** ADDITIVE pure static on StockSageIndicators (StockSageIndicators.swift), composing three confirmed primitives with NO edits: donchian (:209), isBreakout (:219), annualizedVolatility (:118). Verified the advisor currently NEVER consults donchian/isBreakout, so this is the natural place to add breakout selectivity. Computes short-window vol (last shortWindow closes) and long-window vol (last longWindow closes); qualifies a long breakout TRUE only when (a) price strictly clears the look-ahead-free Donchian upper band AND (b) shortVol/longVol >= minExpansion (vol expanding). Caller still builds the channel on bars EXCLUDING the current bar (the documented donchian/isBreakout contract). Returns nil when bars insufficient or longVol<=0 — never fabricates.

**signature:** nonisolated static func qualifiedBreakout(price: Double, highs: [Double], lows: [Double], closes: [Double], donchianPeriod: Int = 20, shortWindow: Int = 10, longWindow: Int = 40, minExpansion: Double = 1.0, periodsPerYear: Double = 252) -> Bool?

**test:** Python re-impl of annualizedVolatility (log-return stdev * sqrt(ppy), matching :118-128) + the qualify rule. Expanding-vol series (small then big steps): short/long>1 -> qualify(True,...) True. Contracting (big then small): short/long<1 -> False. No breakout -> False. longVol 0 -> nil-guard -> False. Closed-form.

**caveat:** A regime FILTER on long breakouts, not a price target. Windows/minExpansion are heuristics, not fitted; minExpansion=1.0 only demands non-contracting vol. Shorts/mean-reversion out of scope. nil on thin history must read as 'cannot assess'. Caller MUST still pass a channel built on bars excluding the current bar.

**edgeRationale:** Donchian breakouts lose most of their edge to false breaks in quiet range-bound tape that immediately revert. Gating on expanding realized vol keeps only breakouts during a real transition to trend (highest documented follow-through and win size) and discards the modal whipsaw. It can only ever turn an existing signal OFF — selection, not extra activity. Chosen over the vol-of-vol brake (rank lower) because it directly improves a signal class the advisor doesn't yet use, rather than broadly braking all entries.

### ⬜ #4 [relative-strength-rank] — Cross-sectional 12-1 relative-strength rank -> top-decile buy candidates
**mechanism:** New pure static crossSectionalMomentumRank on StockSageIndicators (StockSageIndicators.swift), reusing the confirmed timeSeriesMomentum(_:lookback:skipRecent:) at :189 (canonical 12-1: lookback 252, skipRecent 21) as the per-name score, then ranking the watchlist cross-sectionally. Input is [(symbol, closes)] from the histories already fetched in StockSageStore.buildIdeas (verified: histories dict populated at the buildIdeas loop). Names with insufficient bars (timeSeriesMomentum nil) are EXCLUDED and returned separately as `skipped` (never imputed zero). Ranked desc; each gets percentile=(count-rank0Index)/count in (0,1] and isTopDecile = percentile>0.9. Pure rank over existing data, no new fetches. ADDITIVE: every existing call site untouched.

**signature:** nonisolated static func crossSectionalMomentumRank(_ universe: [(symbol: String, closes: [Double])], lookback: Int = 252, skipRecent: Int = 21) -> (ranked: [(symbol: String, score: Double, rank: Int, percentile: Double, isTopDecile: Bool)], skipped: [String])

**test:** Python mirror: tsm(c)=(c[-1-skip]-c[-1-look])/c[-1-look]*100 if len>look else None. 10 names length 260, name i = geometric ramp 100*(1+i*0.01)**t so 12-1 strictly increases with i. Assert exactly 1 name isTopDecile (i=9); ranked strictly desc; a length-200 name lands in skipped not ranked; ranked.len+skipped.len == universe.len. Replicable in pure python.

**caveat:** RELATIVE ranking, not an absolute buy: in a broad bear the top decile may be names merely falling least — pair with the existing trendOK/regime gate before acting. Decile is over WHATEVER priced this scan; if the feed dropped half the universe (ideasMissing) the 'top 10%' is of a shrunken cohort, so surface the analyzed count. Ranks unstable when scores near-tied.

**edgeRationale:** Cross-sectional 12-1 momentum (Jegadeesh-Titman/Asness) is among the most replicated equity anomalies precisely because it is relative: concentrating capital in the top decile has documented positive forward drift over 1-3mo, and the skipRecent=21 removes the 1-month reversal that contaminates raw momentum. Capping candidates to the top decile also prevents position-sizing dilution across low-rank names. Ranked below the breakout qualifier because it surfaces candidates rather than fixing/gating an existing decision, but it is high-confidence, well-evidenced, and purely additive.

### ⬜ #5 [entry-timing] — Pullback-to-support entry gate (skip extended breakout chases)
**mechanism:** New pure helper in StockSageIndicators.swift composing the EXISTING donchian (:209) and atr (:84). Verified: advise scores entries on SMA/RSI/MACD and never consults donchian/isBreakout, so a buy can fire after price has run far above the breakout level. The gate measures extension = (price - channel.upper) / atr; an entry is 'timely' only when extension <= maxATRExtension (default 1.0). Wiring is additive: advise gains a defaulted `requireTimelyEntry: Bool = false`; when true AND highs/lows present, a buy-family action whose extension exceeds the cap is blocked (isBuy=false / demoted). Default false => the Store path (advise via history, verified at buildIdeas) and close-only callers stay byte-identical. The backtester (which decides on bar i, fills next-bar open — verified) opts in by passing requireTimelyEntry: true.

**signature:** nonisolated static func entryExtensionATR(price: Double, channel: (upper: Double, lower: Double), atr: Double) -> Double?  // (price - channel.upper)/atr; nil when atr<=0
nonisolated static func isTimelyBreakoutEntry(price: Double, channel: (upper: Double, lower: Double), atr: Double, maxATRExtension: Double = 1.0) -> Bool

**test:** Python: 25-bar highs whose Donchian(20) upper (max EXCLUDING the decision bar) = 100.0, atr=2.0. entryExtensionATR(101,...) == 0.5 and isTimelyBreakoutEntry True; price=104 -> 2.0 -> False; price=99 (pullback below band) -> -0.5 -> True; entryExtensionATR(...,atr=0) is None. Closed-form max()/subtraction/division.

**caveat:** Filters TIMING only, never direction or universe; only real fetched OHLC drives the band/ATR (nil when atr<=0). The 1.0-ATR cap is a fixed heuristic, not per-symbol fitted; opt-in (default false) so callers/tests stay byte-identical. Backtested improvement is on historical data, not predictive.

**edgeRationale:** Breakout fills degrade monotonically with entry extension: the further above the cleared level you buy, the worse your realized R under the SAME 2:1 ATR-stop geometry (stopTarget, StockSageAdvisor.swift:246). Requiring price within ~1 ATR of support makes every taken trade enter near the structural stop, maximizing reward:risk and cutting late-chase losers — it removes trades, never adds them. Ranked below the candidate screens because it needs the most call-site plumbing (a new advise param threaded through the backtester) for its win, though that plumbing is genuinely additive.

### ⬜ #6 [correlation-filter] — Effective-independent-bets from existing correlations (one number subsuming avgCorrelation + cluster widgets)
**mechanism:** One ADDITIVE pure static on StockSagePortfolioAnalytics (StockSagePortfolioAnalytics.swift) composing the EXISTING averageCorrelation (verified at StockSagePortfolioAnalytics.swift:234, takes [[Double]] return series) over the same aligned holding vectors the analytics suite already builds. Effective N = N / (1 + (N-1)*rhoBar) where N = holdings count, rhoBar = averageCorrelation. nil when series.count < 2; clamped to [1, N] so a negative rhoBar can never report MORE independent bets than holdings. No new data source, no network, no caller change.

**signature:** nonisolated static func effectiveBets(_ series: [[Double]]) -> Double?  // N/(1+(N-1)*rhoBar), rhoBar from averageCorrelation (:234); nil if count<2; clamped [1,N]

**test:** Python mirror of Pearson corr + averageCorrelation + effectiveBets. three_same=[base,base,base] (rho=1) -> 1.0; eff([base]) -> None; three_indep -> result in [1.0, 3.0]. Closed-form.

**caveat:** rhoBar is an AVERAGE pairwise correlation over whatever overlapping history exists — it smears over a single tight cluster, so two genuinely-linked names hidden among diversifiers can still gap together; pair with the existing CorrelationCluster.largest clique note, don't replace it. Correlations rise in crashes, so effectiveBets OVERSTATES independence exactly when it matters. nil (not 0/1) when <2 holdings so the UI shows n/a. NOTE: the spec's claimed Store wiring (lines 441-443 / a `correlation: CorrelationMatrix` field) was not verifiable in the section read — the helper itself is sound and additive regardless of where the aligned vectors are sourced.

**edgeRationale:** Positive expectancy by sizing reality, not ticker count: an owner holding 8 names at rhoBar 0.8 owns ~1.4 independent bets and is ~5x more exposed to a single regime shock than 'I hold 8' implies. One honest effective-N figure recalibrates total heat and position sizing (pairs with StockSagePortfolioHeat). It is a portfolio risk read rather than an entry/expectancy filter, so it ranks below the trade-level edges, but it is trivially buildable and high-value for drawdown control.
