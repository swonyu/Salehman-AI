# OSRS money bughunt (wv2v508ni, 2026-06-22)

2 CONFIRMED (Java parity inspected but UNVERIFIED). #1 (min-margin floor parity) DONE. #2 (stale OSRS price shown as live — highTime/lowTime parsed but unused) = sacred only-real-data, NEXT. RE-VERIFY vs source.

### ✅ DONE #1 [high] — Swift GE flip list has no min-margin filter; surfaces sub-floor flips the RuneLite plugin (source of truth) hides at default config
**failingInput:** item sell(high)=130, buy(low)=100, buyLimit=10000: Swift tax=2 -> profit 28 (>0) so it KEEPS the flip and shows it in the 'Fastest flips' strip at gpPerHour = 28*10000/4 = 70,000 gp/hr. The RuneLite Java plugin at its shipped default (minMargin 50) drops it because postTax 28 < 50, so the two surfaces return different flip sets from identical prices.

**fix:** Add minMargin (and, if a volume field is later added, minVolume / minPrice) parameters to StockSageGEFlip.flips and gpPerHour, defaulting minMargin to 50 to match SalehmanGeConfig.minMargin(), and drop flips whose post-tax profit is below it. At minimum, gate StockSageGEFlip.flips on a documented post-tax profit floor instead of every profit>0 flip, and have RuneScapeMarketView.fastestFlipsStrip pass that threshold. Note: RuneScapeItem/RuneScapeListing currently carry no daily-volume field, so the volume gate cannot be replicated without adding a volume data source — only the margin floor is achievable today. (Java filter behavior at FlipFinder.java:92-103 and defaults minMargin 50 / minVolume 1000 in SalehmanGeConfig.java:27-30 — UNVERIFIED per instructions, but inspected and they do match the claim.)

### ⬜ #2 [high] — OSRS row shows a stale Sell price and net-of-tax flip margin as live; per-quote trade timestamps are parsed but consumed nowhere
**failingInput:** /latest for id 20997 (Twisted bow) = {high:1500000000, highTime:<now>, low:1200000000, lowTime:<now-432000>}: listingRow renders Sell 1.20B in live-green and a net-margin chip '+288.0M' (300M raw - 12M tax) with help text 'NET flip margin after the 2% GE sell tax', presented identically to a fresh spread, while the 1.2B sell leg last traded 5 days ago and the spread no longer exists. The same stale low feeds the per-row gp/hr (297-300) and accessibility label.

**fix:** Consume the timestamps the service already parses. (1) In listingRow, compute the older leg's age from price.highTime/price.lowTime and render an 'Xm old'/'Xh old' caution marker when either leg exceeds a staleness threshold (parallel to the Java maxStaleMinutes default of 60). (2) When the limiting leg is stale, drop the net-margin chip or de-emphasize/label it 'stale - may not fill' instead of styling it like a fresh live spread; likewise gate or annotate the per-row gp/hr. (3) Reword headerSubtitle (96-102) so 'updated HH:mm' is unambiguously the fetch time, not a per-quote freshness claim, since the 'Live' banner (131-132) currently implies every quote is live. (Java parallel: FlipFinder.oldestTradedTimestamp + maxStaleMinutes filter at FlipFinder.java:113-124 and 'Xm old' label in SalehmanGePanel.java:108 — UNVERIFIED per instructions, but inspected and they match.)
