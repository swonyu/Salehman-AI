# A11y + visual audit (wu8rtkq6m, 2026-06-22)

14 findings — glanceability, color-not-only-signal, VoiceOver. #5(flat-as-green) + #7(verdict a11y labels) DONE. RE-VERIFY vs source.

### ⬜ #1 [high] — Money-velocity card button silences weekly-$ and weekly-R in VoiceOver
**file:** /Users/saleh/Desktop/Salehman AI/Salehman AI/Views/MarketsView.swift:2437
**fix:** The explicit .accessibilityLabel("Money velocity summary; tap for the best opportunity") at 2437 overrides every child Text, so the Best-now EV (2372), Fastest R/day (2375), Est./week R (2378), and the +$X/week projection (2384) are all inaudible. Replace it with a label built from the same live values: var parts=["Money velocity"]; if let sym=s.bestSymbol,let ev=s.bestEV { parts.append("best now \(sym), estimated EV \(String(format:\"%+.2f\",ev)) R") }; if let sym=s.fastestSymbol,let v=s.fastestVelocity { parts.append("fastest \(sym), \(String(format:\"%+.2f\",v)) R per day") }; if let wk=s.weeklyR { parts.append("estimated \(String(format:\"%+.1f\",wk)) R per week running the top 3") }; when the $/week branch (2382) is shown append "about plus \(Int(usd)) dollars per week at a \(Int(acct)) dollar account, \(rp) percent risk, an estimate". Set .accessibilityLabel(parts.joined(separator:". ")) and add .accessibilityHint("Tap for the best opportunity"). These are the single most important money figures on the card and are currently spoken as nothing.

### ⬜ #2 [high] — Best-opportunity card button reads EV only — R:R, win est., size, and the shares/$-at-risk line are silenced
**file:** /Users/saleh/Desktop/Salehman AI/Salehman AI/Views/MarketsView.swift:2273
**fix:** The explicit label at 2273 is only "Best opportunity: {symbol}, estimated EV {ev} R", which overrides the collapsed child Text — so the R:R (2250), win estimate (2251), suggested Size % (2253) and the "Size it now" sizer line (2260, the concrete shares + dollars-at-risk) are never spoken. Extend the label with the rendered metrics: append ", reward to risk \(String(format:\"%.1f\",ev.rewardR)) to 1, win estimate \(Int(ev.winProbEstimate*100)) percent"; when idea.advice.suggestedWeight>0 append ", suggested size \(String(format:\"%.1f\",idea.advice.suggestedWeight*100)) percent"; and inside the same guard that renders the Size-it-now branch (2257-2264) append ", \(StockSagePositionSizer.summaryLine(ps, riskPct: rp))". Keep the "estimate, not advice" framing to match the visible copy. The VoiceOver user currently hears the EV but none of the actionable risk numbers.

### ⬜ #3 [high] — "Size it now" leverage warning is color-only (amber vs green)
**file:** /Users/saleh/Desktop/Salehman AI/Salehman AI/Views/MarketsView.swift:2260
**fix:** At 2262 the Size-it-now line is tinted warningSoft (amber) when ps.pctOfAccount>100 and successSoft (green) otherwise — that amber-vs-green is the ONLY signal the suggested size needs margin/leverage. The underlying StockSagePositionSizer.summaryLine text (e.g. "50 shares ≈ $500 at risk (140% of acct)…") never contains the word "leveraged". When ps.pctOfAccount>100, prepend a glyph+word so the go/no-go reads without the tint: change line 2260 to Text((ps.pctOfAccount>100 ? "⚠︎ Uses leverage (>100% of acct) — " : "") + "Size it now: " + StockSagePositionSizer.summaryLine(ps, riskPct: rp)). Keep the existing color as a secondary cue. (Pairs with rank 2: the spoken sizer line will then also carry the leverage word in VoiceOver.)

### ⬜ #4 [high] — Rebalance $-trade amounts render at 9pt (mvFont9) — the actual dollars to trade are the smallest text on the card
**file:** /Users/saleh/Desktop/Salehman AI/Salehman AI/Views/MarketsView.swift:731
**fix:** Each instruction (e.g. "BUY $1,240 of AAPL (32%→40%)") at 731-733 is the concrete dollar amount to trade yet renders at .font(.system(size: mvFont9, design:.monospaced)) (9pt) and is fully tinted successSoft/danger (not white), so the money figure sits below the ~11pt glance floor and at the same size as the caption above it (729). Promote the $ amount to ≥11pt semibold and de-tint the figure: render the line as two Texts — the "$X of SYM" portion at .font(.system(size: max(11, mvFont9+2), weight:.semibold, design:.monospaced)).foregroundStyle(.white), with the direction (BUY/SELL) word carrying the success/danger tint, and the (current%→target%) parenthetical staying at mvFont9 .secondary. This keeps the dollar number at white-level contrast and makes it the most prominent thing on the row.

### ✅ DONE #5 [high] — "Flat" daily-change is green+up-arrow in signalCard but neutral in the heatmap and sparkline (three thresholds for one field)
**file:** /Users/saleh/Desktop/Salehman AI/Salehman AI/Views/MarketsView.swift:1794
**fix:** signalCard uses let up = change >= 0 (1794) then colors successSoft/danger (1818) and picks arrow.up.right/down.right (1810) — so a 0.00% change reads GREEN with an up-arrow. The SAME field (sym.latest?.changePercent) is rendered neutral for |change|≤0.05 in heatColor (1675-1679) and for |change|≤0.1% in sparkColor (3188-3193, whose comment explicitly warns against a "fake green gain"). Introduce one shared helper, e.g. private func changeColor(_ pct: Double) -> Color returning successSoft above +0.05, danger below −0.05, else DS.Palette.textSecondary, plus a direction helper returning arrow.up.right / arrow.down.right / arrow.right (or minus) on the same band. In signalCard replace the binary up with this three-state classification for both the icon (1810) and tint (1818), reusing the ±0.05 constant already used by heatColor so all three surfaces agree on "flat". Pull the 0.05/0.1 thresholds into one named constant to keep them in lockstep.

### ⬜ #6 [medium] — Compounding tracker gain/loss shown only by green/red (no arrow, no +/−, ×1.00 break-even not self-evident)
**file:** /Users/saleh/Desktop/Salehman AI/Salehman AI/Views/MarketsView.swift:1069
**fix:** The compounding headline (1072) and sparkline (1075-1077) are tinted successSoft when comp.finalMultiple>=1 and danger otherwise — color is the only cue distinguishing a losing path (×0.85) from a winning one (×1.40), and the ×1.00 break-even convention isn't visible. Make direction explicit in text on the headline: prefix an SF Symbol — HStack { Image(systemName: up ? "arrow.up.right" : "arrow.down.right").font(.system(size:11,weight:.bold)); Text(String(format:"Compounded to ×%.2f at %.0f%%/trade", comp.finalMultiple, comp.fraction*100)) } — or append " (grew)"/" (shrank)" to the string. The sparkline can keep its tint; the number just needs a shape/word cue independent of color.

### ✅ DONE #7 [medium] — Leverage and gap-risk verdicts speak the raw ⚠︎ glyph and have no accessibility label
**file:** /Users/saleh/Desktop/Salehman AI/Salehman AI/Views/MarketsView.swift:2718
**fix:** The leverage verdict Text("⚠︎ " + lev.verdict) (2718) and the gap-risk verdict Text("⚠︎ " + gap.verdict) (2728) have no .accessibilityLabel, so VoiceOver reads the U+26A0 glyph as "warning" jammed onto the front of these safety-critical (can-lose-more-than-account / 20%-gap-through-stop) lines. Their sibling ⚠︎ lines (brake 2418, concentration 2424) already carry clean labels at 2421/2427 — match that pattern: add .accessibilityLabel("Leverage warning. \(lev.verdict)") after 2718 and .accessibilityLabel("Gap risk warning. \(gap.verdict)") after 2728, dropping the glyph and prefixing a spoken word so the embedded at-risk dollar figures stay intelligible.

### ⬜ #8 [medium] — Position-sizer dollar figures render at 12.5pt with 9pt low-contrast secondary labels (ideaMetric)
**file:** /Users/saleh/Desktop/Salehman AI/Salehman AI/Views/MarketsView.swift:3149
**fix:** ideaMetric is the shared renderer for the highest-stakes numbers — the sizer's At-risk $ / Notional $ / % acct / Shares (2704-2708) and the allocation board's $ at risk / $ notional / EV (2318-2322). The value is 12.5pt (3152) and the label that disambiguates "At risk" (real loss) from "Notional" (gross exposure) is mvFont9≈9pt at .secondary (~white@0.55) (3151) — too small/low-contrast for a distinction that, if confused, is a real money error. Raise the value to .font(.system(size: max(13, mvFont9+4), weight:.semibold, design:.rounded)).monospacedDigit() and the label to .font(.system(size: max(10, mvFont8+2), weight:.semibold)).foregroundStyle(DS.Palette.textSecondary) (white@0.66 instead of .secondary). One edit lifts both the sizer panel and the allocation board; .monospacedDigit() also aligns columns of dollar figures.

### ⬜ #9 [medium] — Estimated weekly-$ gain is a money number drawn at 9pt in low-contrast secondary tint
**file:** /Users/saleh/Desktop/Salehman AI/Salehman AI/Views/MarketsView.swift:2384
**fix:** The "≈ +$X/week at $Y acct, Z% risk" projection (2384-2386) renders at mvFont9 (9pt) with DS.Palette.textSecondary (white@0.66, DesignSystem.swift:45) — ~4:1 on the dark canvas, below the glance floor for the dollar amount. The dim tint is intentional ("an estimate, not a realized gain") and should stay, but lift the size floor: render at .font(.system(size: max(11, mvFont9+2), weight:.medium)) and keep textSecondary (do not jump to white). If you want the figure itself to read while the framing stays neutral, give just the "+$X/week" substring white.opacity(0.82) and leave the "at $Y acct, Z% risk" caption at textSecondary. Same treatment applies to the fast-lane twin at 2497.

### ⬜ #10 [medium] — Weekly-R estimate is green on the Fast Lane strip but neutral on the Money-Velocity summary card
**file:** /Users/saleh/Desktop/Salehman AI/Salehman AI/Views/MarketsView.swift:2491
**fix:** The same forward estimate (expected weekly R) is colored inconsistently: fast lane (2492-2494) renders "≈ %+.1fR/week …" in successSoft (green); the money-velocity summary card renders s.weeklyR via summaryStat (2378) whose value is .white (2458) — neutral. The project already decided these projections should read neutral, not as realized gains — the weekly-DOLLARS lines on both cards use textSecondary with the explicit "an estimate, not a realized gain" comment (2386, 2499). So the green R on the fast lane contradicts the app's own treatment. Fix to neutral on both: change the fast-lane weekly-R tint at 2494 from DS.Palette.successSoft to DS.Palette.textSecondary, matching the neutral summary stat and the neutral $/week lines. (Alternatively thread a color param through summaryStat and tint both — but neutral-everywhere is the consistent choice given the existing rationale.)

### ⬜ #11 [low] — Sizer "% acct" leverage flag relies on red tint (numeric value shown, no shape/word for the 100% line)
**file:** /Users/saleh/Desktop/Salehman AI/Salehman AI/Views/MarketsView.swift:2707
**fix:** The "% acct" ideaMetric (2707-2708) switches its value color to danger red when leveraged (pctOfAccount>100) vs white otherwise. The value ("140%") is shown so it's not strictly color-only, but red is the alarm that 100% has been crossed and a color-blind owner gets no shape/word cue. Append a marker to the value: ideaMetric("% acct", String(format: "%.0f%%", ps.pctOfAccount) + (leveraged ? " ⚠︎" : ""), color: leveraged ? DS.Palette.danger : .white). Low priority because the adjacent leverage verdict line (2718, see rank 7) already states it in words once rank 3/7 land.

### ⬜ #12 [low] — Per-position cost-basis line uses caption2 .secondary — the entry price you bought at is the weakest-contrast number in the row
**file:** /Users/saleh/Desktop/Salehman AI/Salehman AI/Views/MarketsView.swift:638
**fix:** In each holdings row the "N sh @ <costBasis>" line (638-639) is .font(.caption2).foregroundStyle(.secondary) — ~11pt but at ~white@0.55 on the dark/hover-tinted row, the weakest contrast in the row, while the value (14pt white, 643) and P&L (caption, 648) read fine. The cost basis is the reference you need to judge whether the live P&L beside it is good. Bump contrast: keep caption2 but use .foregroundStyle(DS.Palette.textSecondary) (white@0.66), or split so the numeric "@ <cost>" is .white.opacity(0.7) while "N sh" stays .secondary. Low priority relative to the sizer/rebalance figures.

### ⬜ #13 [low] — Same R/day velocity metric rendered in three different colors across surfaces
**file:** /Users/saleh/Desktop/Salehman AI/Salehman AI/Views/MarketsView.swift:2479
**fix:** StockSageExpectedValue.velocity (R-per-day) is tinted three ways: fast-lane strip "%+.3fR/day" in successSoft (2479-2480), money-velocity "Fastest" stat in .white via summaryStat (2374-2375 / 2458), and the idea detail sheet "≈ %+.3fR/day velocity …" in .secondary (2935-2939). Tapping a green fast-lane row lands on a grey detail-sheet value for the same number — three colors undermine the sense that color carries meaning. Since velocity here is always a positive-EV figure (sign already conveyed by the EV chip), make it neutral everywhere: change the fast-lane velocity tint at 2480 from DS.Palette.successSoft to DS.Palette.textSecondary to match the detail sheet. (If green is intended to mean "positive velocity = good," instead apply successSoft in all three places — but neutral is the lower-risk unification.)

### ⬜ #14 [low] — Money-velocity summaryStat tiles have no accessibility grouping (latent if the card is ever un-buttoned)
**file:** /Users/saleh/Desktop/Salehman AI/Salehman AI/Views/MarketsView.swift:2455
**fix:** summaryStat (2455-2461) renders label/value/sub as three bare Texts with no .accessibilityElement/label, unlike its sibling ideaMetric (3149-3155) which ignores children and supplies "\(label) \(value)". Today the tiles sit inside the money-velocity Button so they aren't separately focusable, but the "Est./week" tile pairs value "%+.1fR" with the qualifier sub "if you run top 3" — if this helper is ever reused outside a button, or the button label is fixed by combining children, those would read as two disconnected fragments and the load-bearing "top 3" qualifier could be missed. Make it self-describing: add .accessibilityElement(children:.ignore) and .accessibilityLabel("\(label): \(value), \(sub)") to the VStack. This also makes the rank-1 money-velocity-button fix read naturally if it ever folds children instead of overriding.
