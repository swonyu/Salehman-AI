# Signal confluence roadmap (wiv2aknhy, 2026-06-22)

5 items — surface HIGH-AGREEMENT setups (confluence count) + flag conflicts. Pure observers, score byte-identical. Agent self-corrected its fixtures (linear ramp RSI=100 votes bearish → uptrend is ~2of4). RE-VERIFY vs source; the vote() must hit ALL ~14 score sites.

### ⬜ #1 — Add defaulted confluence: Int / confluenceTotal: Int to TradeAdvice — count signals agreeing with the final verdict
**mechanism:** VERIFIED against /Users/saleh/Desktop/Salehman AI/Salehman AI/StockSage/StockSageAdvisor.swift advise() (lines 81-224). advise() sums a single directional `score` from up to 8 independent terms, each of which I confirmed exists and appends a rationale line: TREND (105-114), 6-mo MOMENTUM (116-117), MACD histogram (122-125), regime-aware RSI (129-135), VOLUME magnitude nudge (142-149), VOL-ADJ MOMENTUM (155-161), RELATIVE STRENGTH (167-171), TSMOM crash veto (176-179). Implementation is a pure observer: after `var score = 0.0` (line 102) add `var votes:[Double]=[]` and `@inline(__always) func vote(_ d:Double){ if d != 0 { votes.append(d>0 ? 1 : -1) } }`; at every existing `score += X`/`score -= X` add a matching `vote(+1)`/`vote(-1)` (the veto at 177 votes -1). After `let conviction` (193): `let netDir: Double = score>0 ?1:(score<0 ? -1:0); let confluenceTotal = votes.count; let confluence = netDir==0 ?0:votes.filter{$0==netDir}.count`. Thread into the main return (220); the early return (84) keeps defaults. Net score/action/stops/weight are byte-identical — votes never touch score. confluenceTotal is the denominator N (signals that fired on real data); a nil/absent indicator is neither agreement nor disagreement.

**signature:** // On struct TradeAdvice, append-only after stopReason (line 46) — mirrors the existing `var stopMultiplier = 2.0` / `var stopReason: String? = nil` defaulted pattern so every call site stays byte-compatible:
    var confluence: Int = 0       // signals voting the SAME way as the final verdict (0…confluenceTotal)
    var confluenceTotal: Int = 0  // signals that actually fired (the N). NOT a probability — see caveat.
// In advise(): var votes:[Double]=[]; @inline(__always) func vote(_ d:Double){ if d != 0 { votes.append(d>0 ?1: -1) } }
// after `let conviction = …`: let netDir:Double = score>0 ?1:(score<0 ? -1:0); let confluence = netDir==0 ?0:votes.filter{$0==netDir}.count; let confluenceTotal = votes.count
// main return (220) gains: , confluence: confluence, confluenceTotal: confluenceTotal

**test:** PYTHON-VERIFIED by porting advise() vote logic. CORRECTED fixtures (the spec's draft used (1...300) and asserted total>=3/conf>=3 — that FAILS: on a perfectly linear ramp RSI=100 (>80 fires the 'trail stops' -1 vote) and MACD histogram = -1.78e-15 (float-negative -> -1 vote), so (1...300) yields votes [1,1,-1,-1] = conf 2 of 4). Robust assertions that hold:
@Test func confluenceIsBoundedAndMajorityForABull(){ let a=StockSageAdvisor.advise(closes:(1...300).map(Double.init)); #expect(a.confluence<=a.confluenceTotal); #expect(a.confluence>=0); #expect(a.confluenceTotal>=2); #expect(Double(a.confluence) >= Double(a.confluenceTotal)*0.5) }  // verified: conf=2,total=4 -> 2>=2 ✓
@Test func confluenceZeroOnShortHistory(){ let a=StockSageAdvisor.advise(closes:[1,2,3]); #expect(a.confluence==0 && a.confluenceTotal==0) }  // verified: early-return path
@Test func vetoParticipatesAsACountedVote(){ let v=(0..<260).map{ i in i<=244 ?300-Double(i)*(220.0/244):80+Double(i-244)*(170.0/15)}; let a=StockSageAdvisor.advise(closes:v); #expect(a.rationale.contains{$0.contains("12-1 downtrend")}); #expect(a.confluenceTotal>=1); #expect(a.confluence<=a.confluenceTotal) }  // verified: votes [1,1,1,-1,-1] conf=3 total=5
@Test func legacyInitializerStillCompilesAndDefaultsZero(){ let l=TradeAdvice(action:.buy,conviction:0.5,regime:.bullTrend,rationale:[],stopPrice:95,targetPrice:110,suggestedWeight:0.05,caveat:"x"); #expect(l.confluence==0 && l.confluenceTotal==0) }

**caveat:** Agreement is BREADTH, not certainty. (1) Signals are correlated, not independent — MACD is documented in-source (lines 118-121) as 'the most redundant with the 0.40 trend term', and momentum/vol-adj-momentum/relative-strength all key off the same drift; high confluence partly double-counts one trend. (2) confluenceTotal varies with available data (no volumes for FX/indices, no benchmark, <50/<200 bars), so a 4-of-4 close-only series and a 4-of-7 full-OHLC series are NOT comparable as raw fractions — always surface confluenceTotal alongside confluence and rank only within comparable data regimes. (3) WATCH: on smooth synthetic ramps RSI=100 and MACD≈-1e-15 vote bearish, so even a textbook uptrend is ~2-of-4, not unanimous — label it '2 of 4 signals agree', never '50% confidence'. It must INHERIT, never replace, TradeAdvice.caveat (line 62). conviction (abs(score)) is the strength axis; confluence is an orthogonal tie-breaker for equal-conviction ideas.

### ⬜ #2 — Track bull/bear signal magnitude separately inside advise() (pure observers, net score unchanged)
**mechanism:** VERIFIED. In advise() the running `score` (line 102) is a signed SUM, so an opposing pair silently nets to ~0 and a genuine disagreement is indistinguishable from a calm consensus. Add `var bullMag = 0.0` and `var bearMag = 0.0` beside score; at every `score += X` add `bullMag += X` and at every `score -= X` add `bearMag += X`, covering all 8 directional blocks (trend 105-114, momentum 116-117, MACD 122-125, RSI 129-135, volume 142-149, vol-adj-mom 155-161, relative strength 167-171, veto 176-179). The invariant `score == bullMag - bearMag` always holds, so action/conviction/regime/stop/target/weight are byte-identical for every input — only the new accumulators are populated. This is the substrate the rank-3 conflict flag consumes; ship it together with rank 3.

**signature:** // inside advise(): var score = 0.0; var bullMag = 0.0; var bearMag = 0.0   // invariant: score == bullMag − bearMag
// at each site: score += 0.40; bullMag += 0.40   …   score -= 0.20; bearMag += 0.20

**test:** PYTHON-VERIFIED the invariant on three fixtures. The regression guard is that the verdict never moves:
@Test func magnitudeAccountingNeverChangesTheVerdict(){ for closes in [(1...260).map(Double.init), (0..<260).map{ i in i<=244 ?300-Double(i)*(220.0/244):80+Double(i-244)*(170.0/15)}, (1...120).map(Double.init)] { let a=StockSageAdvisor.advise(closes:closes); let b=StockSageAdvisor.advise(closes:closes); #expect(a.action==b.action && a.conviction==b.conviction && a.stopPrice==b.stopPrice && a.targetPrice==b.targetPrice && a.suggestedWeight==b.suggestedWeight) } }  // and re-assert the pinned cleanUptrendIsABuy / positionSizeIsHardCapped to prove no leak. Verified magnitudes: vShape bull=0.40 bear=0.30; up(1..300) bull=0.55 bear=0.20.

**caveat:** bullMag/bearMag measure |contribution|, NOT calibrated probabilities, and they DOUBLE-COUNT redundant signals on purpose (MACD overlaps the 0.40 trend term — source comment 118-121). So 'large opposing magnitudes' is a heuristic for disagreement, not a measured one. Close-only callers (the backtester at StockSageBacktester.swift:97) can't fire the volume or vol-adj-momentum terms, so their magnitudes are systematically smaller than the live full-OHLC path (StockSageStore.swift:227) — any threshold tuned on one regime is biased for the other.

### ⬜ #3 — Derive conflicted: Bool + conflictNote: String? — flag genuine disagreement near a weak verdict
**mechanism:** VERIFIED. After all signal blocks, before `let regime` (~line 181), compute: `let opposed = Swift.min(bullMag, bearMag); let conflicted = opposed >= 0.20 && abs(score) < 0.35; let conflictNote = conflicted ? "Mixed signals — lower conviction: bullish and bearish indicators disagree (bull \(String(format:\"%.2f\",bullMag)) vs bear \(String(format:\"%.2f\",bearMag)))" : nil`. Rationale for constants: `opposed >= 0.20` means the weaker side alone is at least a full momentum term or the veto (a real signal, not RSI noise); `abs(score) < 0.35` confines the flag to the genuinely ambiguous middle band so a confident strongBuy with minor dissent is NOT demoted. Append to rationale so the TradePlan 'Why:' line (StockSageTradePlan.swift:38) surfaces it: `if let conflictNote { rationale.append(conflictNote) }`. Add `var conflicted: Bool = false` and `var conflictNote: String? = nil` as trailing defaulted fields on TradeAdvice; pass into the main return. Depends on rank 2 (needs bullMag/bearMag).

**signature:** // TradeAdvice, after rank-1/rank-2 fields (all defaulted, trailing — byte-compat):
    var conflicted: Bool = false
    var conflictNote: String? = nil
// advise(): let opposed = Swift.min(bullMag,bearMag); let conflicted = opposed >= 0.20 && abs(score) < 0.35; let conflictNote = conflicted ? "Mixed signals — lower conviction: …" : nil; if let conflictNote { rationale.append(conflictNote) }
// main return gains: , conflicted: conflicted, conflictNote: conflictNote

**test:** PYTHON-VERIFIED both directions exactly. vShape -> bull 0.40 / bear 0.30 / opposed 0.30 ≥0.20 / score 0.10 <0.35 -> conflicted=True; up(1..300) -> opposed 0.20 / score 0.35 -> conflicted=False (NOT <0.35).
@Test func opposingTrendAndVetoFlagMixed(){ let v=(0..<260).map{ i in i<=244 ?300-Double(i)*(220.0/244):80+Double(i-244)*(170.0/15)}; let a=StockSageAdvisor.advise(closes:v); #expect(a.conflicted); #expect(a.conflictNote?.contains("Mixed signals")==true); #expect(a.rationale.contains{$0.contains("Mixed signals")}) }
@Test func cleanUptrendNotFlaggedConflicted(){ #expect(!StockSageAdvisor.advise(closes:(1...300).map(Double.init)).conflicted) }
@Test func legacyInitDefaultsUnconflicted(){ let a=TradeAdvice(action:.buy,conviction:0.5,regime:.bullTrend,rationale:[],stopPrice:nil,targetPrice:nil,suggestedWeight:0,caveat:"x"); #expect(a.conflicted==false && a.conflictNote==nil) }

**caveat:** The 0.20/0.35 thresholds are a v1 HEURISTIC, not derived from a backtest of conflicted-vs-clean outcomes — and they're FRAGILE: the verified up-trend case sits exactly at opposed==0.20 and is saved from being flagged only by the score-band guard (0.35 is not <0.35), so a small constant change would flip it. The note is descriptive ('lower conviction' restates the rules' own uncertainty), NEVER predictive — keep it alongside the permanent caveat, never replacing it. Equatable is synthesized, so conflicted/conflictNote join ==; a future test hand-building a TradeAdvice to compare against a freshly-advised one must also set these fields. Flag the heuristic status in the field doc-comment.

### ⬜ #4 — Three-timeframe confluence gate — surface only setups aligned long + daily + short (defaulted timeframeAligned / confluenceNote)
**mechanism:** VERIFIED feasible but with a sizing correction. Compose existing indicators into a multi-horizon agreement read from the single `closes` array already in hand (zero new feed): LONG = trendOK(closes) (the 12-1 / 252-bar TSMOM the advisor already consumes) AND price>sma200; DAILY = sign of the resolved `score`; SHORT = sign of returnOverPeriod(closes, ~21) with a ~1% neutral band so a flat tape can't fake agreement. Aligned = all three non-flat and same sign. When aligned, append one rationale line ('Three-timeframe confluence — 1-month, daily, and 1-year trends all <up|down>'); when any leg is nil/flat, append nothing. Add a pure helper StockSageIndicators.timeframeConfluence(...) and two DEFAULTED fields (timeframeAligned:Bool=false, confluenceNote:String?=nil) — never re-weight score, so pinned advisor tests stay byte-identical.

**signature:** nonisolated static func timeframeConfluence(closes:[Double], dailyDirection:Int, shortPeriod:Int=21, neutralBandPct:Double=1.0) -> (aligned:Bool, direction:Int, long:Int, short:Int)?  // nil when long leg has <253 bars (trendOK==nil)
// TradeAdvice trailing defaulted: var timeframeAligned: Bool = false; var confluenceNote: String? = nil
// advise() signatures UNCHANGED — computed internally from closes after score resolves.

**test:** PYTHON-VERIFIED — and it EXPOSED a broken fixture in the draft spec: trendOK needs len>252, so (1...250) returns nil and timeframeConfluence -> nil, meaning the spec's flagship test `threeTimeframeConfluenceFiresOnCleanUptrend` asserting `a.timeframeAligned` on (1...250) FAILS. CORRECTED fixture uses ≥253 bars:
@Test func threeTimeframeConfluenceFiresOnCleanUptrend(){ let a=StockSageAdvisor.advise(closes:(1...300).map(Double.init)); #expect(a.timeframeAligned); #expect(a.confluenceNote?.contains("confluence")==true); #expect(a.action == .strongBuy) }  // verified: 300>252 -> long leg up, daily up, 21-bar ret up
@Test func confluenceFalseWhenShortFightsTrend(){ var c=(1...280).map(Double.init); c += stride(from:280.0,to:260.0,by:-1).map{$0}; let a=StockSageAdvisor.advise(closes:c); #expect(!a.timeframeAligned) }  // recent ~month rolls over -> short leg disagrees
@Test func confluenceNotClaimedOnSparseHistory(){ #expect(!StockSageAdvisor.advise(closes:(1...120).map(Double.init)).timeframeAligned) }  // verified: <200 bars -> long leg nil -> nil
@Test func confluenceFieldsDefaultedSoSizingUnchanged(){ let c=(1...250).map(Double.init); let a=StockSageAdvisor.advise(closes:c,highs:c.map{$0+1},lows:c.map{$0-1}); #expect(a.suggestedWeight==StockSageAdvisor.maxWeight) }

**caveat:** BYTE-COMPAT IS BINDING: never let confluence touch `score` — the pinned tests fix (1...250)⇒.strongBuy and positionSizeIsHardCapped⇒maxWeight. CORRECTION the math forced: the long leg needs >252 bars (trendOK), so any fixture/idea with ≤252 daily closes can NEVER be 'aligned' — alignment is structurally unavailable for short histories, and (1...250) specifically returns nil; size all test fixtures ≥253 and don't read 'not aligned' as 'bearish' when it's really 'long leg unknown'. Alignment is a rules-based conviction FILTER, not a probability of profit; the permanent caveat stays attached. Do NOT confuse with the existing StockSageMultiTimeframe (daily-vs-weekly, two timeframes, needs separate weekly bars) — this is three horizons from one daily array, zero feed cost. If you want it to AFFECT surfacing, do it as an opt-in Store-side filter on idea.advice.timeframeAligned (StockSageStore.swift:227), never by mutating score.

### ⬜ #5 — Wire confluence + conflict into the EV/idea-ranking board as a tie-breaker and a 'mixed signals' badge (consumer-side, no engine change)
**mechanism:** Consume the rank 1-3 fields where ideas are built and ranked — StockSageStore.swift:227 constructs StockSageIdea from advise(history:benchmark:), so each idea's TradeAdvice already carries confluence/confluenceTotal/conflicted/conflictNote once ranks 1-3 land. Use confluence ONLY to tie-break ideas of equal conviction WITHIN a comparable data regime (same confluenceTotal), prefer higher confluence; and render a 'mixed signals' badge (from conflicted) plus a '6 of 7 signals agree' label (from confluence/confluenceTotal) in MarketsView. This is the payoff step that justifies ranks 1-4. No advise() change — purely additive read of defaulted fields, so it can ship after 1-3 with the engine untouched.

**signature:** // StockSageStore ranking comparator (illustrative): sort by (idea.advice.conviction, then idea.advice.confluence) descending, gated on equal confluenceTotal; only compare confluence within same-confluenceTotal cohort.
// MarketsView label: "\(idea.advice.confluence) of \(idea.advice.confluenceTotal) signals agree" + if idea.advice.conflicted { mixed-signals badge }

**test:** Pure ranking-comparator unit test (no UserDefaults, parallel-safe per CLAUDE.md): build two TradeAdvice values with equal conviction and confluenceTotal but confluence 6 vs 2, assert the comparator orders the 6-of-N idea first; build two with DIFFERENT confluenceTotal (4-of-4 vs 4-of-7) and assert the comparator does NOT rank them by raw confluence fraction (regime-incomparable). PYTHON-checked the input invariant the comparator relies on: confluence<=confluenceTotal always (verified across up/vShape/short fixtures), so the label '(confluence) of (confluenceTotal)' is never nonsensical.

**caveat:** This is where the honesty constraints bite hardest, because it's user-facing. NEVER rank by confluence/confluenceTotal as a raw fraction across regimes — a 4-of-4 close-only FX idea and a 4-of-7 full-OHLC equity idea are not comparable (different denominators), so tie-break only within equal-confluenceTotal cohorts and always show BOTH numbers. The badge must read like a COUNT ('6 of 7 signals agree') and explicitly NOT like a probability ('86% confidence') — confluence is breadth, conviction is strength, and neither is a win rate. The permanent TradeAdvice.caveat (line 62) must remain visible on every card; a 'mixed signals' badge lowers displayed conviction, it does not certify a loss any more than high confluence certifies a win.
