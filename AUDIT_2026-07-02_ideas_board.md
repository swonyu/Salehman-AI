# IDEAS Sub-Tab Audit — Final Synthesized Report

**Scope:** StockSage engine (idea path) + MarketsView Ideas region + MarketsTodayActionsCard + tests. 7 read-only passes (A–G), 2026-07-01/02, working tree incl. uncommitted wave-11.
**Honesty-floor sanity check:** every CRITICAL/HIGH was re-checked; **none** recommends removing an honest nil-return or load-bearing guard (the two CRITICALs recommend *adding* a nil/provenance path, which strengthens the floor). **No downgrades required.** One caution noted inline on F35 (dead-guard removal → prefer assert form).

---

## 1. Ideas-surface map

IDEAS SURFACE (verified 2026-07-02, working tree incl. uncommitted wave-11)

**ENGINE — `/Salehman AI/StockSage/` (71 files; idea path):**
- **Feed:** QuoteService/QuoteCache → StockSageStore.swift (1138 ln; `struct StockSageIdea` L6, `buildIdeas` L402) — calls Advisor.advise per symbol w/ calibration+benchmark, folds ExecutionTiming/ReturnShape/VolStability/VolRegime notes into rationale.
- **Scoring:** Advisor (TradeAdvice, conviction 0–1, half-Kelly weight) ← Indicators (pure, nil-on-short-history = honesty floor) + Regime (sizingBias) + MultiTimeframe (lazy weekly) + ExecutionTiming (advisory).
- **Rank/EV:** ExpectedValue (ev/velocity/fastLane/bestOpportunity/netCostFloorFlag/earningsRankFlag) + ConvictionCalibration (measured vs assumed win%) + NetEdge (cost gate, netRR) + VelocityHistory/CompoundingHorizon.
- **Sizing:** Kelly (maxFraction 0.20) → CapitalAllocator → PositionSizer; VolRegime brake; CorrelationCluster/ClusterCheck/Precheck.
- **Exits/plan:** RewardRisk (note now "R:R %.1f gross" — uncommitted), TradeGate (new `rrIsNet` display flag — uncommitted), TradePlan, PartialLadder, TrailingStop/TimeStop/Drawdown.
- **Sheet context:** Earnings, Seasonality, Liquidity, WhatIf, GapRisk, Leverage, Glossary (owns centralized MoneyVelocityCopy strings — good).
- **Deliberately UNWIRED (confirmed):** AllocationOptimizer, Pyramid, Kelly.CostProfile/portfolioCap, donchian/isBreakout, volumeConfirmation, relativeStrengthEnabled (ablated).

**VIEWS — MarketsView.swift (5278 ln; Ideas region 2696–5278):** ideasSection 2698 → ideasHeader 2861 (calibration banner, Copy CSV, sort/filter chips ~2740s) → summaryChip 2806 → ideaCard 2981 (badges; context-menu Copy trade plan → fullPlanText) → bestOpportunityCard 3325 / capitalAllocationCard 3436 / calibrationChip 3501 ("win% assumed|measured · n=X", wave-11) / moneyVelocityCard 3513 / bestOpportunityCTA 3647 / fastLane 3710–3864 → fullPlanText 4315 (unified copy text) → ideaDetailSheet 4374 (Why → EV+chip → gate 4475 + sizer → numerics → ladder/backtest → context; wave-11 pinned safeAreaInset CTA bar 4913–5010 w/ 3rd gate copy 4983) → helpers ideaMetric 5042, convictionMeter 5051, adaptivePrice 795. Plus MarketsTodayActionsCard (PositionSizer/TodayPlan/gateBadge).

**TESTS — 84 `Salehman AITests/StockSage*Tests` + MarketSortTests:** every in-scope engine module has a matching test file. View-layer logic (fullPlanText, triplicated gate derivation, calibrationChip) has NO direct test coverage. Zero TODO/FIXME/HACK in scope.

**MAP DRIFT — no structural drift** (all 78 mapped modules exist, incl. StockSageRewardRisk.swift; wave-11 entries updated). Hardcoded line refs stale after the +318-line MarketsView diff: generateSignal "2486"→2506, allocate "3351"→3438, Leverage "4126"→4228, mtf display "4513"→4537, refreshMultiTimeframe "4669"→5030; RewardRisk consumer refs 4313/4572/2769/2806/2832/3077/3170 all shifted.

---

## 2. Findings table

Dedupes are noted as "passes converged"; severity = highest across converged passes. Citations for F01/F02 re-verified against the working tree (chip Label at MarketsView:3503; identity guard at ConvictionCalibration:479).

| ID | Pass | Sev | file:line | Evidence | Suggested direction |
|----|------|-----|-----------|----------|---------------------|
| F01 | B+D | **CRITICAL ✅RESOLVED 2026-07-04** (clamp, §5 #1) | StockSageConvictionCalibration.swift:479 (+ ExpectedValue:66-70, Store:647) | Thin-journal guard (30–43 closed trades) returns `buildIdentity` (`mapFn: { $0 }`) **unvalidated** — downstream `winProbEstimate` treats any non-nil calibration as the win-prob source, so conviction is rendered as P(profit), bypassing the 0.35+0.23·c prior (0.65-conv → winProb 0.75 vs ~0.50; EV 1.25R vs 0.50R at 2:1); inflates cost-gate/bestOpportunity/rank; silently overrides an OOS-validated backtest fit; identity is never scored against the nil-prior; header's "strictly more conservative" claim inverted for conviction ≥ ~0.45. Test-locked (CalibrationSelectorTests:152-170). | Return nil from the too-thin branch (callers keep prior/backtest fit), or carry an `isIdentity`/method marker that winProbEstimate treats as "assumed", or clamp identity ≤ prior. **Owner decision** — selector owner-activated 2026-06-27 and test-locked; reconcile research/INDEX "identity conservative floor" claim. |
| F02 | B+D | **CRITICAL ✅RESOLVED** (provenance `method`→"assumed", §5 #1) | MarketsView.swift:3503 (also 2872-2875, 3966-3972, 3101-3103, 4421-4423) | "win% measured · n=X" chip + 4 sibling labels key **solely on `calibration != nil`** — identity (measured from zero outcomes) and Platt (central MLE, module header says NOT conservative) display as "measured … (conservative, monotonic)". Estimate-shown-as-measured. No provenance field exists to distinguish. (D rated CRITICAL, B HIGH — kept highest.) | Add a provenance/kind field (identity/beta/isotonic/platt) to StockSageConvictionCalibration; key measured-vs-assumed wording + the "conservative" claim on it; add HonestyGuard test pinning chip semantics (currently zero label coverage). |
| F03 | G+B+D | **HIGH** | StockSageExpectedValue.swift:679 (+ MarketsView:3543/3552/3558/3757; playbook :824/828) | `expectedWeeklyR` sums **gross** velocities; "Est./week %+.1fR" and "≈ +$X/week" sit unlabeled beside "Fastest … R/day **net**" on the same card — intra-card gross/net mix; playbook prints the net fastest with no "net" label (inverse drift in the copyable artifact). | Compute weekly figures from netVelocity **or** append "gross, before costs" to card + fast-lane strip + playbook + glossary. **Owner decision** (changes/relabels a headline number; netting creates a false "fading" step vs prior gross VelocityHistory snapshots). |
| F04 | C+D+G | **HIGH** | MarketsView.swift:4199, 4327, 4474/4982, 3516 vs 3340/3654/3868 | Same @AppStorage sizer fields parsed two ways: sheet sizer/fullPlanText/what-if/gate fallbacks use raw `Double()` (comma-unaware, `?? 0.01`), newer surfaces use `StockSageInput.positiveAmount/percent`. Account "10,000": cards size concretely while the sheet says "invalid", copied plan omits the size line, and the on-screen gate evaluates at the 1% fallback — cross-surface number contradictions; sheet gate can say Clear while the copied plan says blocked. | Unify every read on the wave-11-blessed StockSageInput helpers (a computed `parsedRiskFraction`/`parsedAccount` on the view used by all consumers). |
| F05 | F | **HIGH** | StockSageStore.swift:402 | `buildIdeas` — the idea-build path — has **no direct test**: honesty-note delivery ("⚠ Left-tailed history" L447, "⚠ Whippy volatility" L451, vol-regime note L457, "⏱" timing L471), index exclusion, calibration-changes-only-weight, and the momentumQuality nil-sentinel (L492) are all unpinned; a dropped append fails zero tests. | Add a direct `await buildIdeas(...)` test with crafted histories asserting index exclusion, each note lands in rationale, calibration touches only suggestedWeight, momentumQuality nil for ≤20 closes. |
| F06 | F | **HIGH** | StockSageCapitalAllocator.swift:89 | Multi-brake sizing composition never tested; the per-symbol volRegime brake line (`weight *= mult`) has **zero** coverage — no test constructs an idea with volRegime set; each brake only tested alone. | Composed-brakes test: crisis bias × vol-target × volRegime 0.25 × correlated de-weight × tight maxHeat → assert riskFraction > 0, edge order preserved, totalHeat ≤ cap, zero-share lines dropped not negative. |
| F07 | A | MEDIUM | MarketsView.swift:4361, 4475, 4983 | Gate + net-R:R derivation triplicated (fullPlanText / sheet body / CTA chip), manually "mirrored"; chip comment at 4967 says "Reuse the gate already computed" but recomputes (chipNetRR/chipRR/chipGate); map records a prior divergence from exactly this. | Extract one `gateInputs(for:)` helper used by all three sites; fix the misleading comment (see F25). |
| F08 | A+G | MEDIUM | MarketsView.swift:3103, 3203, sort L16, chip L2746 vs 4423 | Half-applied Conviction→"Signal strength" rename: card help + VoiceOver "conviction N percent" + sort case + filter chip still say "Conviction" while the sheet says "Signal strength"; the spoken "N percent" framing is the exact P(win) misreading wave-9 removed from the sheet. | Pick the canonical term (**owner decision**) and sweep card help, a11y label ("signal strength N of 100"), sort case, filter chip in one pass. |
| F09 | B+D+G | MEDIUM | StockSageTodayPlan.swift:29-32, 104-110 | Both TodayPlan call sites pass **net** R:R (`netRR ?? gross`) to TradeGate without `rrIsNet:` — net figure renders under plain "Reward:risk", user-visible in MarketsTodayActionsCard blocked/caution rows and a11y labels; contradicts the sheet's "Net reward:risk (after est. costs)" for the same quantity. Deferral acknowledged in the uncommitted dev-log, but the *label* (not just verdict) reaches the UI. | Mirror the sheet's rule: resolve netRR separately, pass `rrIsNet: resolvedNetRR != nil` in build() and rankedActions(). **Owner decision** on timing (consciously deferred in wave-11). |
| F10 | C+D | MEDIUM | StockSageInput.swift:12 | `clean` strips every comma as a thousands separator → decimal-comma "2,5" (intended 2.5%) parses as **25%** and passes the 100 cap — StockSageInput surfaces size at 10× intended risk while Double() surfaces in the same sheet fall back to 1%: two different wrong answers from one keystroke. | Treat digit,1-2-digits (no period) as decimal separator or reject as ambiguous (honest-hint nil); add boundary test. **Owner decision** (locale policy for a Saudi-first app; affects every money field). |
| F11 | C | MEDIUM | StockSageStore.swift:857 | `refreshLaneCorrelation` records the fingerprint even when the fetch failed (laneCorrelation nil) — guard at :854 blocks retries, so the fast-lane row shows "Correlation — fetching…" permanently after one network miss. | Record fingerprint only on non-nil result (mirror refreshTrailingStop/refreshLiquidity cache-on-success), or add an explicit failed/"unavailable — retry" state. |
| F12 | C | MEDIUM | StockSageStore.swift:646 | `convictionCalibration` is a computed property running a full journal re-fit per read; 33 read sites per MarketsView body render (inside per-card ForEach); rank comparators recompute key() twice per comparison. Breaks at ≥30 conviction-tagged closed trades (split + IRLS + PAV + OOS selector per read, on MainActor). Jank-scaling, no crash. | Cache the fit, invalidate on journal mutation; decorate-sort-undecorate rank keys. **Owner decision** on invalidation granularity (changes when a just-closed trade affects on-screen EV). |
| F13 | C | MEDIUM | StockSageQuoteService.swift:160 (+ pbxproj `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`) | fetchHistory/fetchHistories/parseHistory are MainActor-isolated — the 6-way task group hops each child back to main for JSON parsing (~250 bars × 6 arrays per symbol; 5y for backtests); only buildIdeas is detached. Correct (no race), jank-scaling only. | Mark fetch/parse statics `nonisolated`/`@concurrent`. **Owner decision** — shared infra consumed by out-of-scope surfaces, crosses COORDINATION.md lanes. |
| F14 | C+D | MEDIUM | MarketsView.swift:4837 (+ Store:14-15/481-482, PortfolioAnalytics:153-154) | Detail-sheet cluster check correlates ~2-day down-sampled sparks positionally with no date alignment (the spark's own doc warns against this; `datedReturns`/`alignByDate` exist for exactly this) — cross-calendar pairs (Tadawul/US, crypto/equity) bias toward 0 → false green "adds diversification"; sibling precheck row uses real daily closes, so two concentration verdicts in one sheet can disagree. | Feed the cluster check date-aligned real returns (reuse precheck's cached data or alignByDate), or label as coarse and suppress the green affirmation for cross-calendar pairs. |
| F15 | G+B | MEDIUM | MarketsView.swift:4335/4361-4365 vs 4475-4481, 4966 | fullPlanText's gate block is conditioned differently than the screen's: (a) copied plan **omits** the gate entirely for stop-only ideas the sheet gates with "No target set" caution (gate lives inside `if let stop, let target`); (b) copied plan **prints** a gate verdict for sell/reduce ideas the sheet never displays a gate for (screen gates buy-family only). | Compute the gate in fullPlanText with the same condition as the on-screen call (hasStop from stopPrice, rewardToRisk nil when no target), and align the buy-family scoping in both directions. |
| F16 | F+G | MEDIUM | StockSageTradeGate.swift:47/53 (+ tests) | Wave-11 `rrIsNet` labeling has **zero tests** (grep: 0 hits) and the wave is uncommitted; the docstring's load-bearing invariant ("pass true ONLY when already net … NOT the `?? gross` fallback") and the rr<0 "net-negative" fail branch are unreached by any test (lowest tested rr = 0.8); dev-log says "Test fixtures updated: None". | Before committing wave-11: pin rrIsNet=true label prefix, default byte-identity, rewardToRisk −0.3 → blocked + "costs exceed the reward" label, and (extractable) the resolved-vs-fallback rule. |
| F17 | F | MEDIUM | StockSageVolRegime.swift:115 | The 0.25 hard sizing floor ("sizing never goes to 0") — the load-bearing lower bound of the brake chain — is asserted by no test (suite pins ≤1.0 and monotonicity only). | Pin the floor: extreme percentile/vol fixture asserting sizingMultiplier == 0.25 exactly and regime() result ≥ 0.25. |
| F18 | F | MEDIUM | StockSageRewardRisk.swift:27 | Wave-11 "gross" honesty suffix in RewardRisk.note is unpinned — RewardRiskTests never asserts `note` at all (rationale: "the two must never appear to say the same thing with different values"). | Assert note contains "gross" + the %.1f breakeven decimal; consider extending HonestyGuardTests' structural caveat sweep to cover it. |
| F19 | F | MEDIUM | MarketsView.swift:4315 | fullPlanText — the unified copy artifact carrying wave-11's caveat/net-R:R/gate additions — is a private view function no test can reach; asymmetric with the engine-hosted, parse-tested TodayPlan.copyAllText. | Extract the string assembly into a nonisolated static engine helper (TradePlan/TodayPlan pattern) + port parse-style tests. **Owner decision** (crosses views lane, grows engine surface). |
| F20 | F | MEDIUM | MarketsView.swift:2823 | Ideas-board `displayedIdeas` sort/filter/search pipeline untested — .conviction/.rr sorts, 3 filters, minConv, search are view-local (MarketSortTests pins only the watchlist); .rr sort duplicates the sheet's capped rewardRisk math by comment only. | Extract a pure static ideas-in→ideas-out helper; pin .rr cap parity, .sells includes .reduce, search fields, empty-message constraint priority. |
| F21 | A | LOW | MarketsView.swift:4423 | Find-replace artifact: "Signal strength — a rules-based signal strength, not a probability…" — label repeats itself. | Reword when the F08 canonical-term decision lands. |
| F22 | A | LOW | MarketsView.swift:2879, 3103, 3496, 3509, 4423; Glossary:56 | Assumed win-prob band "35–58%" hardcoded at 6 display sites with 3 formattings (en-dash/hyphen/prose) while the band lives in ExpectedValue. | Expose one display constant (ExpectedValue or MoneyVelocityCopy) and interpolate. |
| F23 | A+G | LOW | MarketsView.swift:4472, 5278, ~4966 | Stale/incorrect cross-reference comments: "velocity sibling at 2491" (now :3516), EOF changelog "line ~2610" (ideaCard now 2981), and the chip's false "Reuse the gate already computed" (it recomputes). | Replace hardcoded line numbers with symbol names; delete the EOF changelog comment (fact already in DEVELOPMENT_LOG.md); reword chip comment to "recomputed with the same inputs" (or moot via F07). |
| F24 | A | LOW | MarketsView.swift:3698 vs 5042 | Near-duplicate label-over-value helpers (summaryStat/ideaMetric) 1,344 lines apart with divergent a11y (ideaMetric has accessibilityElement, summaryStat none). | Fold into one parameterized helper (optional sub-line) for uniform a11y; summaryChip stays separate. |
| F25 | A | LOW | MarketsView.swift:2022, 3650, 3851 | Inline `hasSuffix("-USD")` crypto predicate duplicated at 3 view sites vs canonical StockSageAllocation.assetClass. | Route through `assetClass(sym) == "Crypto"` so a future suffix (-USDT) can't diverge card vs engine. |
| F26 | A | LOW | MarketsView.swift:3427 vs 5268-5272 | Twin Copy-plan buttons declared "identical" in the CTA doc-comment but labeled/worded differently ("Copy today's plan" vs "Copy plan", differing help). | Unify label + help (or centralize in MoneyVelocityCopy) so "identical" is true. |
| F27 | B | LOW | StockSageExpectedValue.swift:186 + MarketsView:4592/4345 | Short-financing note keys on rate alone: FX/index sells have 0 hold days → $0 financing modeled, yet the cost line claims "+ ~300bps/yr short financing" was included. | Key the note on `finRate > 0 && finDays > 0` (sheet line + fullPlanText), or return rate 0 with days 0. |
| F28 | B | LOW | StockSageTradePlan.swift:44 | Copied TRADE PLAN's R:R line omits the "gross" tag the on-screen RewardRisk note now carries; unlabeled break-even % computed on gross. Mitigated by fullPlanText's separate labeled net-R:R line. | Add "gross" to TradePlan.text's R:R line to match the sheet. |
| F29 | B | LOW | MarketsTodayActionsCard.swift:89 (+ TodayPlan:139, MarketsView:4625) | Velocity labeled "gross" on the fast-lane row but unlabeled on the today card, Copy-all export, and the sheet's velocity evidence line — same quantity. | Append "gross"/"raw EV/day, before costs" consistently across the three unlabeled sites. |
| F30 | B | LOW | MarketsView.swift:3533, 3361 (+ playbook :820) | Headline EV on moneyVelocityCard/bestOpportunityCard lacks the "(gross)" tag ideaCard:3036 and sheet:4609 standardized on — same evR, four surfaces, two labeled. | Add "(gross)" (or a shared formatter) on the two cards + playbook line. |
| F31 | C | LOW | MarketsView.swift:5030 | `.task` inserts into mtfFetchCompleted even when the sheet was dismissed mid-fetch (cancellation → nil, execution continues); the view-level @State persists, so reopening skips the spinner and flashes "Weekly timeframe unavailable" while a retry is in flight. Store caching unaffected. | Guard the completion mark with `Task.isCancelled`, or key the spinner off an in-flight set. |
| F32 | C | LOW | StockSageStore.swift:888 (+ Earnings:45, ExpectedValue:297) | Session earnings cache never expires: after the report passes mid-session, `max(0, days)` floors at 0 → .imminent badge + −2000 rank demotion persist for hours. Conservative failure direction (over-warns). | Evict/refetch once `timeIntervalSinceNow < -86_400` in the derived read, mirroring the fetch-time staleness guard. |
| F33 | D | LOW | StockSagePortfolioAnalytics.swift:152 | `correlation()` returns 0 (a claimed value) for n<2 but nil for zero variance — inconsistent "undefined" convention; unreachable today (callers pre-guard). | Return nil for n<2 to match the zero-variance convention; verify pre-guarded callers stay byte-identical. |
| F34 | D | LOW | StockSageAdvisor.swift:302 | ±0.5 boundary asymmetry: +0.5 → StrongBuy (inclusive) but −0.5 → Reduce not Sell; reachable (weights land on 0.05 grid). | Mirror (−0.5 → .sell) or document the deliberate long-side bias at the switch. |
| F35 | D | LOW | StockSageConvictionCalibration.swift:516, 541 | `if train.count >= minTrainSamples` always true (guard at :479 ensures it); dead `width` + `_ = width`. *Caution:* the guard is a de-facto invariant — prefer the auditor's assert option over silent deletion. | Convert to an assert documenting the invariant; delete the dead width pair. |
| F36 | D | LOW | StockSageTradeGate.swift:84 | Any correlation < 0.8 is labeled "Low correlation … adds diversification" — 0.79 is neither; wording, not threshold. | Add a moderate band (0.5–0.8) or state the number without the "low/diversification" claim. |
| F37 | D | LOW | StockSageAdvisor.swift:149 | "6-month momentum" label for windows as short as 100 bars (~4.6 months), contradicting the same commit's own honesty note. | Raise the exact-label threshold to momPeriod == 126 (or ≥120). |
| F38 | E | LOW | StockSageQuoteService.swift:86, 149 | `.urlPathAllowed` leaves '/' raw — "BRK/B" silently rewrites the request path; Earnings uses stricter `.urlHostAllowed` for the identical job. Hygiene only (keyless GET, honest-nil failure). | Unify on one stricter set for path-segment symbol encoding. |
| F39 | E | LOW | StockSageEarnings.swift:49 (= QuoteService:48, + MediaSearch) | Browser UA literal duplicated byte-identically across files — a future Yahoo UA fix could land in only one copy. UA spoof itself is deliberate/documented. | Hoist one shared UA constant. |
| F40 | F | LOW | StockSageFastLaneBoardsTests.swift:53 (impl ExpectedValue:890) | Partially circular threshold test: cryptoRotationDominant's 1.5× asserted with the implementation's own velocity sums; a threshold drift only fails if fixtures happen to straddle it. | Construct fixtures just above/below 1.5× and assert the flag flips. |
| F41 | F | LOW | StockSageStore.swift:1004 | `rankScore` — the board's default .signal ordering — is private with zero tests; a single sign slip silently reorders the board. | Make internal (or wrap) and pin the total order across all six actions + conviction tie-break. |
| F42 | G | LOW | MarketsView.swift:4316 | "Card export is byte-identical to the sheet export" holds only with warm caches — cold context-menu copy lacks chandelier/earnings/liquidity/precheck flags the sheet copy carries seconds later. Honest-nil behavior; the dev-log/map wording overstates. | Fire the same prefetches from the card copy action, or qualify the claim to "identical given the same cache state". |
| F43 | B | INFO | StockSageExpectedValue.swift:62 | `winProbEstimate` docstring overclaims "MEASURED, conservative" for any non-nil calibration — false for identity and Platt. Doc premise of F01/F02. | Reword: conservatism depends on fit path (Wilson-LCB isotonic yes; Platt/identity no). Fix alongside F01/F02. |
| F44 | B | INFO | StockSageExpectedValue.swift:679 | expectedWeeklyR can include a below-net-cost-floor idea's gross velocity (fastLane demotes, never excludes) while the same summary's "Fastest" skips deranked ideas — one rollup, two policies. Companion to F03. | Filter floor-deranked ideas from the weekly prefix, or note the thin-lane inclusion in the caveat. |
| F45 | C | INFO | MarketsView.swift:2888 | Fire-and-forget `Task { sleep(2s); ideasCopied = false }` mutates @State untied to view lifetime — SwiftUI-tolerated, no defect observed. | If touched anyway, use a .task-scoped debounce. No standalone action. |
| F46 | D | INFO | StockSageConvictionCalibration.swift:68 vs research/INDEX.md:38 | Shipped default `candidateSelectorEnabled = true` (owner-activated 2026-06-27) vs INDEX's "off-by-default, default byte-identical". | Spec keeper appends the activation to the INDEX entry so the permanent record matches shipped. |
| F47 | E | INFO | StockSage/* + views + tests (sweep) | Security/hygiene sweep CLEAN: zero secrets/tokens (feed deliberately keyless, ToolPolicy-gated), zero logging calls (no payload/account leak path), no fabricated imports/dynamic-API tricks, SQL/XSS/JWT/CORS/npm N/A, quote cache is public-data-only with honest degradation, plaintext @AppStorage account size acceptable (not a credential, never transmitted). | None — Keychain-only rule holds; recorded as verified-clean baseline. |
| F48 | A | INFO | MarketsView.swift:5045, 4917, 4944 (examples) | Style drift: ~50 raw font-size literals interleaved with the mvFont* token system (incl. wave-11 pinned bar). | Sweep onto mvFont tokens with the next visual-polish wave, not alone. |
| F49 | A | INFO | MarketsView.swift:4374-5010 (densest) | 23+ ephemeral review-session tag prefixes (hc#/aa#/sd#/[AUDIT iterN]/wave-N) baked into comments; the rationale prose itself is load-bearing and good. | Drop tag prefixes on next touch of each block; never a standalone churn commit; do NOT delete the honesty-rationale sentences. |
| F50 | A | INFO | StockSageIndicators.swift | Orientation: 12 production consumers (11+ idea-facing) — apex of the pipeline; nil-on-short-history contracts are the honesty floor. | Treat nil/guard contracts as frozen API; edits require full Indicators+MathInvariant suites + downstream parity. |
| F51 | A | INFO | StockSageExpectedValue.swift | Orientation: 12 production consumers — every rank, badge, and dollar figure flows through it; source of the F22 band literal. | Same frozen-contract treatment. |
| F52 | A | INFO | MARKETS_TAB_MAP.md:40 + consumer entries | Hardcoded line refs stale after +318-line diff (2486→2506, 3351→3438, 4126→4228, 4513→4537, 4669→5030; RewardRisk consumer refs shifted). No structural drift; wave-11 entries correct. | On next map touch, replace raw line numbers with member names (the map's own updated-TradeGate-entry convention). |
| F53 | F | INFO | Salehman AITests/ (85 files, sweep) | Baseline: suite genuinely assertion-rich; no smoke-only tests; honest-nil branches well pinned at engine level (VolRegime/ReturnShape/Advisor short-history). | None — read F05/F06/F16-F20 as targeted gaps, not systemic weakness. Do NOT weaken the nil-branch tests — they enforce the honesty floor. |
| F54 | G | INFO | StockSageExpectedValue.swift:69 (+ NetEdge:154, Advisor:229-243) | Regression sweep clean: winProbEstimate formula byte-identical since creation; 50:1 cap mirrored in both modules; floor/sentinel band unchanged; varScalar attenuation-only; the wave-8/9/11 honesty-string deletions were strengthening relabels, not dropped caveats. | None — verified-clean baseline for the next iteration audit. |

---

## 3. Summary counts by severity

| Severity | Count | Notes |
|----------|-------|-------|
| CRITICAL | 2 | F01–F02 — both the identity-calibration seam (engine + display); both owner-gated |
| HIGH | 4 | F03–F06 — 2 honesty-labeling, 2 test-coverage |
| MEDIUM | 14 | F07–F20 |
| LOW | 22 | F21–F42 |
| INFO | 12 | F43–F54 (incl. 3 verified-clean baselines: F47, F53, F54) |
| **Total (post-dedupe)** | **54** | 68 raw findings across 7 passes → 12 merges (B+D×2, B+D+G×2, C+D+G, C+D, A+G×2, F+G, B+G, E-sweep×1, A-comments×1) |

Honesty-floor downgrades applied: **0** (no CRITICAL/HIGH recommended removing an honest nil or load-bearing guard).

---

## 4. Embedded directives (ignored)

All passes encountered the same repo directives; none was acted on — this was a read-only audit with no changes made. Unique directives encountered:

- **CLAUDE.md:** "After ANY change … append a dated entry to DEVELOPMENT_LOG.md" — not owed (no changes).
- **CLAUDE.md:** "Keep SOURCE_BUNDLE.md complete … regenerate with `bash tools/bundle_source.sh`" — not owed (no code change).
- **CLAUDE.md:** "NEVER Read SOURCE_BUNDLE.md (~530k tokens)" — complied by all passes.
- **CLAUDE.md:** "Leave it green — build + tests must pass before hand-off" — not applicable (read-only contract forbids builds/tests).
- **MARKETS_TAB_MAP.md:** "Update the relevant entry when you materially change a file" — not applicable.
- **research/INDEX.md:** "Append a dated line / never re-research — extend the entry" — read for context only; no research performed. (Note: F46's INDEX correction is *recommended* for a future write session, not performed.)
- **MarketsView.swift:5278:** trailing comment styled as an instruction/claim to future readers — quoted as a stale artifact (F23), not treated as guidance.

---

## 5. Needs owner decision

1. ✅ **RESOLVED 2026-07-04 (owner-approved "do it") — F01/F02 both closed.** F02 (provenance) shipped earlier: `StockSageConvictionCalibration.method`/`chipTitle`/`isMeasuredFromOutcomes` render identity as **"win% assumed (identity)"**, never "measured" (test 9 `identityProvenanceRendersAssumedNeverMeasured`). F01 (number) closed via the **clamp option**: the thin-split identity (n∈[30,43], no OOS validation) is clamped to the conservative prior in `buildIdentity(clampToPrior:)` — winProb = min(conviction, 0.35+0.23·c) — so it is genuinely "strictly more conservative" (≤ prior everywhere; the inversion for conviction ≥ ~0.45 is gone) and the low-conviction band is untouched (clamp only lowers → no idea promoted). The OOS-VALIDATED identity winner (n≥44) stays raw (`clampToPrior:false` default → byte-identical selector). `priorWinProb` extracted as the single source (F46). Test-lock updated to the clamped contract (CalibrationSelectorTests test 4, hand-derived 0.25/0.5225, falsifiability-probed). research/INDEX.md:38 reconciled.
   *~~Original (for the record):~~ The iter7 selector returning a non-nil identity calibration is owner-activated (2026-06-27) and test-locked (CalibrationSelectorTests:152 asserts small-N → identity, winProb ≈ band midpoint). Options: return nil from the thin branch, add an isIdentity/provenance marker the UI keys "assumed" on, or clamp identity ≤ prior.*
2. **Weekly headline gross→net vs label (F03/F44).** Netting expectedWeeklyR/Dollars shrinks the most prominent money figure on the Markets header and creates a false "fading" step against prior gross VelocityHistory snapshots; labeling "gross, before costs" preserves the number. Both close the finding; pick before any fix.
3. **Decimal-comma input policy (F10, feeds F04).** Locale-aware parsing vs reject-ambiguous vs keep US-style — affects every money field in a Saudi-first app, not just Ideas.
4. **Canonical term: "Conviction" vs "Signal strength" (F08/F21).** Pick the winner before the unification sweep.
5. **TodayPlan rrIsNet threading timing (F09).** Consciously deferred in the uncommitted wave-11 dev-log ("verdict identical" — but the label is user-visible); confirm label parity now vs bundled with the next TodayPlan wave.
6. **QuoteService nonisolated change (F13).** Shared infrastructure consumed by out-of-scope surfaces (Watchlist/Portfolio/Alerts/backtest); crosses COORDINATION.md lanes — needs cross-session sign-off.
7. **convictionCalibration caching granularity (F12).** Memoizing changes when a just-closed journal trade starts affecting on-screen EV (currently instant next render) — product-behavior choice, not mechanical.
8. **View-logic extraction for testability (F19/F20, enables F07/F16 view-side pins).** Moving fullPlanText/displayedIdeas/rrIsNet-resolution into nonisolated engine helpers follows repo convention but crosses the views lane and grows the engine surface — architecture call.
---META---
rawCount: 73
bySeverity: {'MEDIUM': 21, 'LOW': 24, 'INFO': 19, 'CRITICAL': 2, 'HIGH': 7}
