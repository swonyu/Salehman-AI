# First-60-seconds decisiveness plan (wrao0jxsy, 2026-06-22)

6 items — shortest honest path from open to a sized, gated, ready trade. #3 (SAMPLE in copied plan) DONE. RE-VERIFY vs source.

### ⬜ #1 — Auto-scan ideas on open + land on the Ideas board — kills the 3-tap cold-launch funnel to the best sized move
**friction:** On every cold launch the actionable surface (best move + size + gate) is gated behind 3-4 deliberate taps: (1) tap Markets — .task runs store.refresh() which hydrates quotes/regime but pointedly NOT ideas; (2) tap the Ideas segment — @State section defaults to .watchlist (L91) and isn't persisted, so it resets every launch; (3) tap 'Find ideas' — store.ideas is empty until refreshIdeas() runs, so bestOpportunityCard is hidden (bestOpportunity returns nil on empty ideas) and the section shows only the empty prompt. The one automation that does fire spends a network round-trip on quotes but never triggers the ideas scan. This is the gate on EVERY other improvement: until ideas exist, nothing downstream renders.

**fix:** Two lines, real-data-only, no fabrication. (A) Open on the actionable board: change L91 to `init(qaSection: MarketSection = .ideas)`, and at the QASnapshots call sites that build MarketsView() pass `MarketsView(qaSection: .watchlist)` so the 'markets' capture stays stable (the heatmap capture already passes its section explicitly). (B) Auto-scan when empty: inside the existing .task, AFTER the `guard !ProcessInfo…contains('--qa')` (L137, so QA stays offline/deterministic) and `await store.refresh()` (L138), add `if store.ideas.isEmpty { await store.refreshIdeas() }`. refreshIdeas already self-guards: re-entry (isLoadingIdeas L157), ToolPolicy.webToolsDisabledReason (L158-161 so .auto/local-only sets ideasError instead of spending — satisfies the auto-cost-guard), non-destructive on failure (L172-175), and names unfetched symbols via ideasMissing. So on a real feed it ranks real ideas, on no-web it shows the honest 'web off' reason, on partial fetch it keeps the '⚠ N priced · M couldn't be fetched' caveat. Net: 0 taps to the best sized move (vs 3), gate verdict still 1 tap away via 'Copy today's plan'.

**surface:** /Users/saleh/Desktop/Salehman AI/Salehman AI/Views/MarketsView.swift — init L91 (default .watchlist), .task L134-144 (calls only store.refresh()); /Users/saleh/Desktop/Salehman AI/Salehman AI/StockSage/StockSageStore.swift — refreshIdeas() L156-189 is the ONLY populator of store.ideas, and the store init never loads ideas (cold every launch).

**value:** Highest. Cuts cold-launch taps-to-action from 3 (best move+size) / 4 (gate) to 0 / 1 with zero fabricated data — reuses the exact gated, non-destructive refreshIdeas path the manual button uses. It is also the prerequisite: every other finding here renders inside ideasSection/bestOpportunityCard, which are empty until this fires.

### ⬜ #2 — Surface the CLEAR/CAUTION/NO-GO gate verdict inline on the glance cards — stop a no-stop 'Strong Buy' from reading green
**friction:** The three decision-moment surfaces each render a green action capsule (a.action.rawValue, actionColor) + EV/size, but NONE shows the gate verdict — the single most useful discipline output. A setup with no stop, risk over the 2% cap, or negative reward:risk skew therefore appears at a glance as a green positive-EV 'Strong Buy' with zero on-screen warning. Worse, a nil-stop buy is currently invisible: ideaCard just omits the Stop metric (L2129 'if let stop') so undefined-risk reads identical to a sound setup. The verdict exists only after a tap into the sheet, or in the clipboard after 'Copy today's plan'.

**fix:** Add a small gate pill next to the action capsule on the three glance surfaces, reusing the engine and colors verbatim — no new data. (1) Helper `gateVerdict(for:) -> TradeGateVerdict?` returning nil for non-buy actions (mirrors the sheet's buy/strongBuy guard so Hold/Avoid/Sell stay clean) and otherwise calling StockSageTradeGate.evaluate with the SAME inputs the sheet/plan use: hasStop: a.stopPrice != nil; rr from a.stopPrice/a.targetPrice; riskFraction: Double(sizerRiskPct)/100 ?? 0.01; daysToEarnings: store.earnings[symbol.uppercased()]?.daysUntil. (2) `gatePill(_:)` mapping decision→label ('CLEAR'/'CAUTION'/'NO-GO') + the icon/color already in tradeGateView (checkmark.seal.fill+successSoft, exclamationmark.triangle.fill+warningSoft, xmark.octagon.fill+danger). (3) When .blocked, override the green capsule (red-ring or prefix 'NO-GO') so a no-stop Strong Buy no longer reads as a go. (4) As belt-and-suspenders, in ideaCard add a 'No stop' danger chip around L2129 when a.stopPrice == nil for a buy-family action. (5) Carry honesty: gate.caveat via .help/.accessibilityLabel; the full checklist stays in tradeGateView. Real-data-only is automatic — gateVerdict returns nil for non-buy/missing inputs, so a blocked/unknown setup never gets a fabricated all-clear.

**surface:** /Users/saleh/Desktop/Salehman AI/Salehman AI/Views/MarketsView.swift — bestOpportunityCard L2243-2245 (action capsule), ideaCard L2098-2101, moneyVelocityCard 'Best now'; vs the gate's ONLY current invocation inside ideaDetailSheet (tradeGateView, behind a tap). StockSageTradeGate.evaluate at /Users/saleh/Desktop/Salehman AI/Salehman AI/StockSage/StockSageTradeGate.swift L40-87; a nil stop is a hard .fail (L49-51) that forces .blocked (L84).

**value:** High. Moves the go/no-go from a tap-gated modal to the moment of decision, and the BLOCKED-overrides-green rule closes the worst footgun (entering with undefined risk). Depends on rank 1 to have any card to render on.

### ✅ DONE #3 — Mark SAMPLE/stale data in the copied plan — the one artifact pasted into a broker is the one place the caveat is dropped
**friction:** On a cold launch (especially now that rank 1 makes ideas appear automatically) isSampleData is true and idea.price is illustrative. The on-screen sampleBanner is honest (MarketsView L152), but 'Copy today's plan' drops it: the copied string presents 'Best bet: AAPL (Strong Buy) … Entry ~123.45, stop 119, 50 shares' as if real, with no marker. The owner can paste 'BUY AAPL @ 123.45 stop 119, 50 shares' into a broker ticket and act on a number the app invented for illustration. The directive is explicit: never fabricate a move, real-data-only — and the clipboard is exactly where the caveat is missing.

**fix:** Add `isSample: Bool` and optional `asOf: Date?` params to StockSageTodayPlan.build. When isSample, prepend a hard first line: '⚠ SAMPLE DATA — illustrative prices, NOT a live quote. Re-price before any order.' When live, append 'Prices as of <asOf>, may be delayed.' At the call site (MarketsView L2277-2281) pass store.isSampleData and store.lastUpdated. ~6 lines, no engine change, keeps every existing caveat and adds the missing one.

**surface:** /Users/saleh/Desktop/Salehman AI/Salehman AI/StockSage/StockSageTodayPlan.swift — build() signature L14-16 (no freshness flag), header L29, rule L49; call site /Users/saleh/Desktop/Salehman AI/Salehman AI/Views/MarketsView.swift L2277-2281. store.isSampleData defaults true (StockSageStore.swift L35), idea.price is the sample seed until the live feed lands.

**value:** High on honesty/safety, low cost. This is the smallest fix that touches correctness rather than ergonomics — it prevents the single dangerous outcome (acting on fabricated prices). Becomes more urgent precisely because rank 1 makes sample ideas surface with zero taps.

### ⬜ #4 — Put entry/stop/target + an inline sizer on the best card so the full broker-ready quad is visible with zero taps into the modal
**friction:** The best card answers 'how many shares' (when account/risk are set) but not 'where's my stop' or 'where do I take profit' — the two numbers literally entered into the broker live only in the sheet. And the share count itself is conditional on sizerAccount/sizerRiskPct, which are only editable inside the sheet's positionSizerPanel: the FIRST time (or any time cleared) the card silently shows no size and the only fix is tap card → open sheet → scroll → type. The inputs and the output are in different surfaces, so assembling a complete entry/stop/target/size plan costs a tap + a modal.

**fix:** Three additions to bestOpportunityCard, all reusing values already on idea.advice (no new data, Optionals degrade gracefully like the sheet): (1) An entry/stop/target row via ideaMetric — Entry %.2f idea.price; if a.stopPrice → Stop (danger); if a.targetPrice → Target (successSoft). (2) A compact inline sizer row mirroring positionSizerPanel L2694-2697, binding to the SAME @AppStorage keys (Acct $ / Risk %) so the sheet stays in sync. (3) Because journalField is a TextField it cannot live inside the card's outer Button (label L2237) — move the new fields and the gate/stop/target rows OUT of the Button into the surrounding VStack (L2236-2290 already wraps the Button in a VStack), keeping only the tappable summary inside the Button. Net: from seeing the best idea, share count + stop + target + (rank 2's) gate are all on the card with ZERO taps into the modal; the sheet becomes a deliberate drill-down. Keep MoneyVelocityCopy.bestOpportunity (L2265) and summaryLine's 'sizes the LOSS, not a profit promise'.

**surface:** /Users/saleh/Desktop/Salehman AI/Salehman AI/Views/MarketsView.swift — bestOpportunityCard: metrics HStack L2247-2256, 'Size it now' L2257-2264 (gated on Double(sizerAccount)/Double(sizerRiskPct)); the sizer fields live only in positionSizerPanel inside ideaDetailSheet (L2694-2697), and labeled Stop/Target only in the sheet (ideaMetric rows). ideaMetric helper + @AppStorage sizer keys already exist.

**value:** High ergonomics. Together with rank 2 this collapses the on-screen card into the equivalent of StockSageTodayPlan.build (best bet + gate + entry/stop/target + size), so the four facts arrive in one glance instead of fragmented across card and modal. Depends on rank 1 for the card to exist.

### ⬜ #5 — Wire correlation into the today's-plan gate so the copied verdict isn't systematically more lenient than the app's own concentration check
**friction:** build() calls StockSageTradeGate.evaluate without maxCorrelation (it defaults to nil at TradeGate L44), so the concentration check never fires in the copied plan. A trader pasting the 'one motion' plan can be told 'Clear to trade' while the idea is 0.8+ correlated with an existing holding — i.e. doubling an existing bet. The gate verdict in the clipboard is therefore systematically more lenient than the gate the app can compute, exactly where the owner is about to act.

**fix:** Thread the existing max-correlation-vs-book value (already computed via the store's precheck/correlation paths used elsewhere in MarketsView) into StockSageTodayPlan.build and forward it to evaluate's maxCorrelation argument. One added param + one call-site wire-up. If rank 2's gatePill shares the same gateVerdict helper, pass the same correlation value there too so card and clipboard agree.

**surface:** /Users/saleh/Desktop/Salehman AI/Salehman AI/StockSage/StockSageTodayPlan.swift L26-27 (evaluate call omits maxCorrelation) vs the unused correlation branch in StockSageTradeGate.swift L72-76; the store already maintains correlation/concentration data (StockSageStore.swift concentration path).

**value:** Medium-high correctness. Smaller blast radius than ranks 1-4 (only the copied plan / shared gate helper), but it removes a real concentration-blind 'Clear' verdict. Lower than 3 because it's a missing warning, not a fabricated price.

### ⬜ #6 — Translate advice verbs to broker order sides and suppress entry/stop blocks for non-trades; add currency + notional
**friction:** The plan prints 'Strong Buy'/'Reduce', not BUY/SELL + order type — the owner must mentally translate 'Strong Buy → BUY LIMIT' and 'stop 119 → SELL STOP 119'. Worse, the entry/stop block renders even for Hold/Avoid (L37-44 only branches on stopPrice, not action), so a non-actionable idea still produces a fillable-looking 'plan'. Prices are bare floats with no currency on a store that spans non-USD markets (idea.market exists), and the plan never states notional for buying-power. These are extra mental motions on top of the 'one' tap, and a real cross-currency risk on non-US names.

**fix:** In build(): map action→intent once — long (Strong Buy/Buy) → 'BUY (entry), protective SELL STOP (stop)'; Reduce/Sell → 'SELL/EXIT'; Hold/Avoid → emit 'No trade — stand aside' and SKIP the entry/stop/size lines (a small switch, no schema change). Pass idea.market / a currency string into fmt (use StockSageCurrency for the symbol) and add the already-computed ps.notional to the size line. Reuses PositionSize.notional, no new math.

**surface:** /Users/saleh/Desktop/Salehman AI/Salehman AI/StockSage/StockSageTodayPlan.swift L31 (emits a.action.rawValue), L37-44 (entry/stop block emitted regardless of action), fmt L53 ('%.2f', no currency); StockSagePositionSizer already computes notional but build() surfaces only shares + $-at-risk (L41).

**value:** Medium polish on a working button. Genuinely shortens the mental path from copied string to broker ticket and stops emitting fake-fillable plans for Hold/Avoid, but it is the lowest-leverage item: the plan already works and these are translation/formatting refinements, not the cold-launch funnel or an honesty hole.
