# macOS
.DS_Store

# Xcode — user state & build artifacts
xcuserdata/
*.xcuserstate
DerivedData/
build/
*.xcscmblueprint
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/

# Editor / tooling
.claude/
.vscode/

# Local training kit — contains a 1.1 GB embedded llama.cpp clone + models/build
# artifacts. Local-only; not part of the app source. (The small `training/`
# scripts dir IS tracked.)
salehman-training/

# Model weights / large binaries — never commit
*.gguf
*.safetensors
*.bin
*.mlmodelc/

# Local, non-app artifacts moved out of the Xcode source root (DEVELOPMENT_LOG
# 2026-06-08): a nested repo copy + the claude-autocontinue browser extension
# (its own git repo). Kept on disk, but not part of this app — don't track.
External Artifacts/

# Stray build/tooling junk (a grok-polish `git add -A` swept these in 2026-06-09)
__pycache__/
*.pyc
*.profraw
Bash tool output*
