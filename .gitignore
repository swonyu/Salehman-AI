# macOS
.DS_Store

# Xcode — user state & build artifacts
xcuserdata/
*.xcuserstate
DerivedData/
build/
*.xcscmblueprint
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/

# Editor / tooling
.claude/
.vscode/

# Local training kit — contains a 1.1 GB embedded llama.cpp clone + models/build
# artifacts. Local-only; not part of the app source. (The small `training/`
# scripts dir IS tracked.)
salehman-training/

# Model weights / large binaries — never commit
*.gguf
*.safetensors
*.bin
*.mlmodelc/
