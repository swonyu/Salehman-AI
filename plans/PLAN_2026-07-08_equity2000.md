# PLAN — Equity count → 2,000: promote catalogExtra into the analyzed scan (owner-ordered 2026-07-08)

Owner (verbatim): "i want equity count to be 2000" — the previously-gated auto-analyze-2000-on-board
is UNGATED and ORDERED. Target: the ~2,208 catalogExtra US equities join the Find-Ideas scan
(analyzed universe 210 → ~2,418; analyzed equities 174 → ~2,382).

## The three constraints (recon-verified)
1. **Watchdog:** refreshIdeas hard-cancels at 120s (Store ~:347). 2,400 per-symbol history fetches
   at concurrency 6 ≈ 5–15 min wall clock. One global timeout cannot survive.
2. **Throttle:** Yahoo v8 chart is per-symbol; the 2026-07-07 verifier drew 429s at ~1,000 tight
   requests; the app path (concurrency 6, natural pacing) verified 2,420 QUOTES gently, but a
   2,400-history burst repeated daily is real cooldown risk. Mitigation required, not hoped away.
3. **Cache:** HistoryCache at 209 entries = 4.3MB JSON; ~2,400 ≈ 50MB — encode ~seconds (detached,
   acceptable v1), disk fine. Candidate follow-up: sharded/binary encoding (recorded, not v1).

## Stage 1 — scan infrastructure (no universe change; behaviour-preserving at n=210)
- **Chunked progressive scan:** refreshIdeas processes trackedDefs() in chunks (~250): fetch chunk
  → buildIdeas(chunk defs, chunk histories) → MERGE into the board (the retryFailedIdeas merge
  semantics are the tested precedent: replace-by-symbol + stillTracked reconcile + re-sort) → next.
  Watchdog becomes PER-CHUNK (120s each — a stall kills one chunk, not the scan). Board grows live;
  a mid-scan failure keeps completed chunks (partial success is honest success; ideasMissing names
  the remainder at scan end).
- **Ordering invariant:** the FIRST chunk is the legacy 210-core (Saudi-first, Aramco first) so the
  board's most-watched names appear fastest; catalogExtra chunks follow.
- **Scan-end semantics:** the scan-delta baseline write + missingAfterScan + newListings + cache
  save + paper-trades advance + earnings kick run ONCE at scan end over the FULL result (the DEG-01
  carry-forward and CONCURRENCY #3 invariants apply to the whole scan, not per chunk). Alerts
  detect once at end vs the previous full board.
- **Cache-aware skip:** before fetching a chunk, serve symbols whose cache entry is from TODAY
  (savedAt same UTC day + entry present + non-stale per isStale) from cache without a network call.
  Makes same-day re-scans and failure recovery nearly free; the first scan of a day is still full.
- **Pacing:** a small per-request stagger inside fetchHistories' task group (e.g. 100–150 ms per
  slot) — spreads 2,400 requests over ~7–10 min. Tunable constant, documented.
- **Throttle auto-fallback:** if a catalogExtra chunk returns <30% coverage (429-storm signature),
  ABORT the remaining extended chunks, keep everything already committed, banner honestly
  ("Extended scan paused — feed throttling; core results are complete"), and do NOT hammer on.
  Core chunks are exempt from the fallback (they use the existing coverage-guard semantics).
- Tests: chunk merge = old single-shot result at n=210 (equivalence on fixtures); staleness skip;
  fallback trigger math; delta-baseline written once at end (carry-forward preserved).

## Stage 2 — universe promotion (the actual order)
- `worldwide` = build(groups) + build(catalogExtra) US names (dedup; Saudi-first order preserved;
  marketCount reflects it). The catalog stays the superset for search.
- Sweep every "210-name analyzed core" copy string (board header, Trade-ideas card, .help texts —
  grep "210") to the new honest composition wording.
- ideasProgress counter shows n / ~2,400 with the chunk-live board.

## Stage 3 — perf + UX honesty at n≈2,400
- displayedIdeas sorts ~2,400 ideas with ~3 NetEdge evals each per body render (bound once per
  body since round F, but per render). MEASURE first (the perf lens's honest-materiality
  precedent); if a keystroke-lag shows, memoize keyed on (ideas array identity, sort, filters) —
  the JournalCalibrationCache pattern. LazyVStack (round F) already covers row rendering.
- Scan-duration honesty copy: "First scan of the day covers ~2,400 names and takes several
  minutes — results stream in as chunks complete." Never a fake spinner.
- QA fixtures unaffected (seedQAIdeas bypasses the scan; captures deterministic).

## Protocol
Stage per fleet round (sonnet impl in worktree, isolated .dd gates → fable review armed on the
merge/ordering/fallback invariants → my dual-dd gate + capture + pollution checks → ship + CI).
STOP-on-mismatch. The honesty floor governs every banner/counter — partial coverage is always
NAMED, never silently truncated (coverage-guard lineage).
