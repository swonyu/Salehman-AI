# Feed/cache data-layer audit (wujphmi3c, 2026-06-22)

7 CONFIRMED data-LAYER only-real-data gaps (16 agents, verify ran). #1 (parseHistory 0/neg OHLC) DONE — the only one that corrupts a COMPUTED money number. Rest = real-but-mislabeled-as-fresh. RE-VERIFY vs source.

### ✅ DONE #1 [high] — parseHistory accepts zero/negative OHLC closes — they become latestClose and feed price*shares money math
**file:** Salehman AI/StockSage/StockSageQuoteService.swift:175-184
**fix:** In the per-bar loop guard (lines 176-178), add positivity checks mirroring parseChart's price>0 (line 114): change to `guard let ts = number(timestamps[i]), let oo = number(opens[i]), oo > 0, let hh = number(highs[i]), hh > 0, let ll = number(lows[i]), ll > 0, let cc = number(closes[i]), cc > 0 else { continue }`. Leave volume (line 181) unguarded since 0 volume is legitimate for FX/indices. This is purely additive guard conditions; it keeps a 0/negative final bar from becoming closes.last -> latestClose -> StockSageStore.swift:230 StockSageIdea(price:) and :266 currentValue: price * p.shares.

### ⬜ #2 [high] — Failed/partial live refresh leaves stale prices under the green "Live · updated HH:mm" banner — feedError never surfaced once isSampleData==false
**file:** Salehman AI/StockSage/StockSageStore.swift:628-639 (failure branches); UI Salehman AI/Views/MarketsView.swift:345-347
**fix:** Add a published staleness signal the live banner reads. (1) In StockSageStore add `@Published private(set) var lastRefreshFailedAt: Date?`. (2) In both early-return failure branches set it alongside feedError: line 629 (`feedError = "..."; lastRefreshFailedAt = Date(); return`) and line 637 (same). (3) Clear it on success right after `lastUpdated = Date()` (line 648): `lastRefreshFailedAt = nil`. (4) In MarketsView headerSubtitle (lines 345-347) and feedBanner, when `store.lastRefreshFailedAt != nil` render an amber `"⚠︎ \(store.feedError ?? "refresh failed")"` ahead of the green Live line so the green dot + unchanged "updated HH:mm" can never coexist with a known-failed refresh. Additive: existing live path unchanged when the last refresh succeeded.

### ⬜ #3 [high] — Disk quote cache of ANY age is trusted as last-good with no max-age gate and no staleness flag
**file:** Salehman AI/StockSage/StockSageStore.swift:83-91 (loadCachedQuotes); model StockSageQuoteCache.swift
**fix:** Add a max-age ceiling + staleness flag. (1) In StockSageQuoteCache add `var age: TimeInterval { Date().timeIntervalSince(savedAt) }` and `func isStale(maxAge: TimeInterval = 7*24*3600) -> Bool { age > maxAge }`. (2) In StockSageStore add `@Published private(set) var cacheIsStale = false`. (3) In loadCachedQuotes() after line 84, hard-reject obviously-unusable caches: `if cache.age > 30*24*3600 { return }` (keep the sample seed, leave isSampleData=true). Otherwise after line 90 set `cacheIsStale = cache.isStale()`. (4) Reset `cacheIsStale = false` on next live success, next to loadedFromCache=false at line 650. (5) In MarketsView headerSubtitle (345-347) prepend `"⚠︎ STALE "` and surface the calendar date (a `.short`/`.short` formatter, not the HH:mm-only one) when store.cacheIsStale. Note the existing cachedBanner (MarketsView.swift:158) already prints relative age (Xd old) — coordinate wording rather than duplicating.

### ⬜ #4 [medium] — Yahoo regularMarketTime never parsed — every quote restamped Date() at merge; stale/delayed/halted quote looks fresh
**file:** Salehman AI/StockSage/StockSageQuoteService.swift:107-119 (parseChart) and 25-29 (LiveQuote)
**fix:** Parse and carry the feed's real observation time; never default it to now(). (1) Add `let asOf: Date?` to the LiveQuote struct (lines 25-29). (2) In parseChart after line 117 read it from the same meta dict: `let asOf = number(meta["regularMarketTime"]).map { Date(timeIntervalSince1970: $0) }` and pass `asOf: asOf` into the LiveQuote initializer (line 118) — nil only when the feed genuinely omits it. (3) At the two StockSageQuote construction sites (StockSageStore.swift:138 addSymbol and :625 refresh) pass `time: q.asOf ?? Date()` so a real feed time wins over the merge clock. (4) Surface staleness: in the live banner/headerSubtitle (MarketsView.swift:345-347), when the newest quote.asOf is older than a threshold, annotate "delayed/as-of" rather than trusting lastUpdated=Date(). Additive — falls back to current behavior when asOf is nil.

### ⬜ #5 [medium] — Preserved user-watchlist rows are silently stale yet shown under the global "Live · updated HH:mm" header with no per-row freshness marker
**file:** Salehman AI/StockSage/StockSageStore.swift:643-651; row render Salehman AI/Views/MarketsView.swift signalCard (~1846-1935)
**fix:** Badge rows the feed missed this cycle. Preserved rows are re-injected verbatim (line 644 filter) so they RETAIN their original older quote.time — exploit that. (1) Publish `@Published private(set) var staleSymbols: Set<String> = []` and, just before replaceAll (line 647), set it to the uppercased symbols of preservedUserRows; clear it to [] on a fully-covered refresh. (2) In signalCard, when `store.staleSymbols.contains(sym.symbol.uppercased())` render a small "as of HH:mm" / amber stale chip next to the price (timeFormatter on `sym.latest?.time`). Do not change the row's price value — only add the marker. Additive; no behavior change for fresh rows.

### ⬜ #6 [medium] — Partial coverage in [0.5,1.0) silently drops missing symbols with no count or list of what's stale/absent
**file:** Salehman AI/StockSage/StockSageStore.swift:628-639
**fix:** Mirror the ideas board's existing `ideasMissing` honesty. (1) Add `@Published private(set) var quotesMissing: [String] = []`. (2) In refresh() after computing live (line 627), set `quotesMissing = universe.map { $0.symbol }.filter { !liveKeys.contains($0.uppercased()) }` (move/compute liveKeys earlier, currently built at line 643). (3) Surface it in headerSubtitle (MarketsView.swift:345-347) like ideasMissing is surfaced (~line 2083): when non-empty append `" · \(store.quotesMissing.count) symbols couldn't price"`. Additive; the [0.5,1.0) path keeps replacing the board but now discloses the gap.

### ⬜ #7 [low] — On feed-unreachable first launch, refresh() labels SAMPLE prices as "showing the last data"
**file:** Salehman AI/StockSage/StockSageStore.swift:628-631
**fix:** Make the message provenance-aware. Replace line 629's fixed string with a conditional: `feedError = (isSampleData && !loadedFromCache) ? "Couldn't reach the market feed — these are SAMPLE prices, not live." : "Couldn't reach the market feed — showing the last data."`. Only claims "last data" when real cached/live rows are actually on screen. One-line change; the header's separate SAMPLE warning (MarketsView.swift:352-354) stays, so the two messages stop contradicting.
