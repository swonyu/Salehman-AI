# Data-integrity / feed-robustness sweep (wnw1nvskx, 2026-06-23)

5 CONFIRMED data-layer defects (19 agents, 2-skeptic). #1 (Monitor pushed notifications on stale cache — sacred) DONE. RE-VERIFY rest vs source.

### ✅ DONE #1 [high] [realDataViolation] — Monitor pushes live Strong-Buy/Sell notifications computed on STALE disk-cache prices — freshness gate checks isSampleData but not loadedFromCache
**file:** /Users/saleh/Desktop/Salehman AI/Salehman AI/StockSage/StockSageMonitor.swift
**fix:** At StockSageMonitor.swift:79 extend the honesty gate to also exclude cache-loaded boards: `let liveNotify = notify && !StockSageStore.shared.isSampleData && !StockSageStore.shared.loadedFromCache`. loadedFromCache is @Published private(set) (StockSageStore.swift:38), readable from the Monitor, set true in loadCachedQuotes (line 89) and cleared only after a real live refresh commits (line 673) — so this suppresses pushes on stale boards while leaving the labeled in-app board visible. Verified: loadCachedQuotes sets isSampleData=false while loadedFromCache=true (lines 88-89), and refresh() early-returns on web-off (619-622), feed-unreachable (640-643), and coverage<0.5 (648-651) without clearing loadedFromCache or prices, so stale cache persists into runCycle. Add a regression test asserting runCycle(notify:) yields no alert when the store is loadedFromCache.

### ✅ DONE #2 [medium] [realDataViolation] — FX rate has no freshness guard — a stale cached rate converts the whole portfolio USD total/P&L
**file:** /Users/saleh/Desktop/Salehman AI/Salehman AI/Views/MarketsView.swift
**fix:** In fxRatesToUSD (MarketsView.swift:556-563) gate the =X quote on freshness the way GEFlip does (StockSageGEFlip.swift:101 uses isStale). Instead of currentPrice() (which drops latest?.time at line 544), read the latest StockSageQuote for the =X symbol and, when its .time is older than an FX-appropriate max age (e.g. 24-48h for 24x5 FX), either (a) exclude that currency from portfolioTotals exactly like untrackedFXCurrencies (566-570) or (b) attach an FX-specific 'converted at a rate from <age> ago' caveat tied to the USD total — not just the generic cached banner. Reachable on the live-refresh-partial path where only the =X pair failed to update (equities fresh => loadedFromCache=false => no banner). Verified: rate gated only on r>0/inv>0; portfolioTotals (584-585) multiplies BOTH value and cost by it into the headline.

### ✅ DONE #3 [medium] [realDataViolation] — refreshIdeas()/retryFailedIdeas() never re-reconcile against userSymbols after their awaits — a removeSymbol() mid-fetch resurrects the dropped ticker into ranked ideas and the capital allocator
**file:** /Users/saleh/Desktop/Salehman AI/Salehman AI/StockSage/StockSageStore.swift
**fix:** Mirror the guard refresh() already has at line 659. After the awaits and before committing in refreshIdeas() (StockSageStore.swift:165 captures universe pre-await; commits ideas at 183 and ideasMissing at 190 from that stale snapshot), recompute `let stillTracked = Set(trackedDefs().map { $0.symbol.uppercased() })`, then filter: `let ranked = built.filter { stillTracked.contains($0.symbol.uppercased()) }.sorted(...)` and restrict ideasMissing to `stillTracked.contains($0.uppercased())`. Apply the same post-await filter to retryFailedIdeas()'s built/merged (line 213). Verified: removeSymbol (144-150) mutates userSymbols+symbols but never ideas/ideasMissing; store.ideas flows unfiltered into the capital allocator (MarketsView) which emits dollarsAtRisk/notional the user copies. Bounded (user-added tickers only, deliberate two-tap race, self-heals next cycle) but the allocator path makes it a stale money number.

### ⬜ #4 [low] — Partial refresh() blends a stale user-added holding price into the fresh headline portfolio total (value/P&L)
**file:** /Users/saleh/Desktop/Salehman AI/Salehman AI/Views/MarketsView.swift
**fix:** refresh() preserves a skipped user-holding's PRIOR row via preservedUserRows (StockSageStore.swift:664-667) and commits it alongside fresh rows under one lastUpdated; portfolioTotals (MarketsView.swift:576-588) sums its stale price (currentPrice at 543-544 has no freshness check) with fresh holdings and a freshly re-fetched FX rate. Use StockSageQuote.time: in portfolioTotals/portfolioSummary, either exclude holdings whose latest quote time predates lastUpdated by more than a cycle and list them in a 'stale price — excluded' caveat alongside unpricedHoldings (590-592), or keep summing but surface 'N holding(s) showing last-good (stale) price'. Lowest-risk: extend the existing exclusion caveat to distinguish unpriced vs stale. Confined to user-added tickers the feed skips while coverage>=0.5; curated holdings immune.

### ⬜ #5 [low] — Future-dated GE leg timestamp is clamped to age 0, masking provably-untrustworthy data as the freshest spread
**file:** /Users/saleh/Desktop/Salehman AI/Salehman AI/RuneScape/RuneScapeModels.swift
**fix:** In oldestLegAge (RuneScapeModels.swift:47-55) detect a future leg instead of clamping: if `now.timeIntervalSince(oldest) < 0` return nil, and make isStale (59-64) return true for that case — mirroring the existing half-timestamped 'unverifiable -> stale' handling (line 51/62). Currently `Swift.max(0, now.timeIntervalSince(oldest))` turns a future/clock-skewed timestamp (timeval at RuneScapeMarketService.swift:119-122 validates nothing) into age 0, so isStale returns 0>3600=false and listingRow (RuneScapeMarketView.swift:293-294) renders full-opacity '0m old' with no stale warning. Cosmetic — the gp/hr and price numbers themselves stay correct; this only suppresses the protective staleness cue, so low.
